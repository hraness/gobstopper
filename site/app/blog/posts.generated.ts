// Generated from ../../content/blog by scripts/sync-blog.ts. Do not edit.
export const postBodies = {
  "introducing-gobstopper": {
    "html": "<p>Gobstopper inspects Claude Code, Codex, and Devin sessions and prepares compacted transcript copies. It removes stale tool output by a rule you choose, and before it changes anything it stores the original transcript in a local archive. If a compaction drops something you needed, you can search the archive for it or restore the whole session.</p>\n<p><strong>Status:</strong> Latest release: {{release.version}}. That tag predates the safeguards this post describes, so install from the current source on the main branch.</p>\n<h2 id=\"the-problem-it-solves\">The problem it solves</h2>\n<p>Late in a long coding-agent session, much of the context can be old tool output: test logs from runs that have since passed, file listings from before a refactor, a stack trace for a bug that is already fixed. The agent still reads all of it on every turn. Somewhere in that pile is the one error message or constraint the next step depends on.</p>\n<p>Providers offer their own compaction, which usually replaces history with a model-written summary. That makes the context smaller, but a summary can lose detail, and it does not show you what it left out.</p>\n<p>Gobstopper treats compaction as an edit you can inspect and undo. A rule decides which records to shorten. A preview shows what would change. The original bytes go into an archive before a new copy is written, and the copy is checked for structural problems, such as a tool call left without its result, before it is published.</p>\n<h2 id=\"who-it-is-for\">Who it is for</h2>\n<p>Gobstopper is for developers who run long Claude Code or Codex sessions and want a smaller context without silently losing the details the next turn needs. It also suits people who want to measure that tradeoff on their own sessions before trusting any compaction method.</p>\n<p>Use something else if you want a running session's context shrunk in place. A live session's context belongs to the provider process that loaded it, and Gobstopper does not reach into it. For that, use the provider's own <code>/compact</code>; Gobstopper can tell you when a session has crossed your threshold. Devin sessions are supported for inspection and evaluation of an exported copy, not for writing.</p>\n<p>If you use xcb, it already applies Gobstopper's elision policy to drop stale tool output from Claude Code and Codex prompts once context passes a threshold, and it keeps the original output in local history. That plugin is on by default, and you can turn it off.</p>\n<h2 id=\"what-it-does-today\">What it does today</h2>\n<p>Install it from source:</p>\n<pre><code class=\"language-sh\">cargo install --git https://github.com/hraness/gobstopper gobstopper\n</code></pre>\n<p>Find your sessions and preview a compaction at the size you want:</p>\n<pre><code class=\"language-sh\">gobstopper detect\ngobstopper plan &lt;session&gt; --trigger 100000 --floor 30000\n</code></pre>\n<p>The trigger is the context size at which Gobstopper starts to act, and the floor is the size it aims for. <code>plan</code> writes nothing. When it decides not to act, <code>plan --json</code> returns a reason code such as <code>below_trigger</code> or <code>minimum_savings_not_met</code>. One code, <code>strategy_returned_no_plan</code>, means the strategy declined for a reason Gobstopper does not know.</p>\n<p>The simplest rule, <code>elide</code>, replaces stale tool outputs with a short stub, oldest first, until the estimate reaches the floor. The most recent outputs are protected; by default that is the last two. A replaced record reads like this:</p>\n<pre><code class=\"language-text\">[output elided by gobstopper: 512 bytes]\n</code></pre>\n<p>Other built-in rules remove duplicate outputs by content hash, keep only the newest outputs per tool, protect both ends of the transcript, or add a short state card built from session metadata. The built-in rules use local logic and need no model; model scoring for the <code>scored</code> rule is opt-in. You can compare them on the same frozen copy of a session before you pick one:</p>\n<pre><code class=\"language-sh\">gobstopper eval &lt;session&gt;\n</code></pre>\n<p>When you are ready, <code>apply</code> writes a separate compacted copy for Claude Code or Codex. Your original session file is left alone:</p>\n<pre><code class=\"language-sh\">gobstopper apply &lt;session&gt; --strategy elide\n</code></pre>\n<p>Before the copy is published, the source and the candidate bytes go into a content-addressed archive under <code>~/.local/share/gobstopper/vault/</code>. Searching or reading a snapshot checks the stored bytes against their hashes. To find an error message that a compaction removed, search one archived snapshot and read only the matching record:</p>\n<pre><code class=\"language-sh\">gobstopper search-snapshot &lt;snapshot-sha&gt; --query 'exact error text' --json\ngobstopper read-snapshot &lt;snapshot-sha&gt; --record 42 --max-bytes 4096 --json\n</code></pre>\n<p>Search matches literal, case-sensitive text; there is no semantic search. To go back to the whole session, <code>gobstopper undo &lt;session&gt;</code> restores a snapshot into a new fork. <code>gobstopper mcp</code> runs a read-only Model Context Protocol server with tools for recall, history, diffs, plans, and structural checks. It has no tool that changes a transcript. An agent can use the snapshot search and read tools only when you start the server with <code>--allow-transcript-content</code>; archived text the agent retrieves then goes to that agent and its model provider.</p>\n<p>On September 19, 2026, an offline replay ran the <code>compacted</code> strategy over 729 archived sessions from one Mac. Across 73 high-context archived Codex root tasks, it had a median projected context reduction of 36.4% and kept 76.9% of sampled strings. Across all 729 sessions, 637 produced no plan and the median reduction was 0%. These are offline estimates, not billing savings or measures of task accuracy. The <a href=\"/benchmarks\">benchmarks page</a> has every cohort and the method.</p>\n<h2 id=\"where-it-is-going\">Where it is going</h2>\n<p>The roadmap describes Gobstopper as the context-compaction layer for the Hraness agent stack: a policy engine that agent runtimes such as xcb embed, with measurement shared with AI Charts. The intent is to steer provider-native compaction rather than only trigger it, and to compact at natural breaks between tasks instead of at a flat token count. None of this has shipped.</p>\n<p>Some core pieces are checked with more than tests today. The archive's concurrency design has TLA+ models, including deliberately broken variants that must fail; <a href=\"/blog/vault-models-that-fail-on-purpose\">Vault models that fail on purpose</a> walks through them. The limits on an edit plan, such as at most 64 edits and at most one state card per plan, are small Rust functions shared by the production code and Kani proofs; <a href=\"/blog/proofs-for-the-admission-math\">Proofs for the plan-size arithmetic</a> explains what those proofs cover.</p>\n<h2 id=\"limits\">Limits</h2>\n<p>Each of those checks covers a specific component at a stated scope. The TLA+ models check a finite design, not the Rust code. The Kani proofs cover selected arithmetic, not whole programs. Lean proves list laws about transcripts, with finite checks against the Rust code. None of them establish whole-system correctness, behavior after a power loss, or that a provider will accept a copy.</p>\n<p>A compacted copy passing Gobstopper's structural checks does not mean Claude Code or Codex will resume it; test that yourself before relying on it. Released builds cannot ask a provider to compact a live session, and direct rewrites of provider files or stores are turned off. The archive returns a missing detail only when you or your agent ask for it.</p>\n",
    "toc": [
      {
        "href": "#the-problem-it-solves",
        "label": "The problem it solves"
      },
      {
        "href": "#who-it-is-for",
        "label": "Who it is for"
      },
      {
        "href": "#what-it-does-today",
        "label": "What it does today"
      },
      {
        "href": "#where-it-is-going",
        "label": "Where it is going"
      },
      {
        "href": "#limits",
        "label": "Limits"
      }
    ]
  },
  "proofs-for-the-admission-math": {
    "html": "<p>A compaction tool edits the record your coding agent depends on most: the running transcript of what it has read, run, and decided. Gobstopper shortens that transcript by replacing stale tool output with a short stub. If the rule that picks what to replace is wrong at one edge, the damage shows up later, as an agent that has lost the one error message it needed or a session file its provider refuses to reopen.</p>\n<p><strong>Status:</strong> Latest release: {{release.version}}. The limit functions and proofs in this post are on the main branch and are not yet in a tagged release.</p>\n<h2 id=\"code-that-is-right-until-the-long-session\">Code that is right until the long session</h2>\n<p>Much new software is written quickly by a model and checked by a few examples, so it looks finished and passes the tests someone thought to write. This is vibe-coded slop: software that works on the first try and breaks on the second use, usually at an input nobody typed. Build a product on top of enough of it and you have a fragile foundation, where each layer trusts the one below without anyone having looked.</p>\n<p>Limit checks are where this hides best. A rule that says &quot;at most 64 edits per plan&quot; can be written so it quietly allows 65, or refuses the 64th. A running total of token counts can grow past the largest number the computer stores and wrap around to a small one, so a huge transcript reads as tiny. A &quot;keep the most recent outputs&quot; rule can subtract past zero on a short session. These bugs stay hidden on the session you tested with and appear on the longest, oldest session, the one you most wanted compacted.</p>\n<p>The same goes for the edit itself. Replacing old output looks simple, but a careless version can drop a record, shuffle two, change a record you asked it to protect, or leave a tool call whose result has disappeared. In a Claude Code or Codex transcript, every tool result has to follow its call.</p>\n<h2 id=\"what-changes-when-the-edge-cases-are-ruled-out\">What changes when the edge cases are ruled out</h2>\n<p>A proof checks a statement for every input it describes, where a test checks only the examples it was given. When the statement is &quot;this limit check accepts 64 and refuses 65, for every possible number,&quot; there is no untested edge left for that check, because every value was considered, including the largest the machine can hold.</p>\n<p>You no longer have to hope the last test run hit the bad value. When the proof runs against the code that ships, a later change that breaks the rule fails the repository's checks before it reaches your transcripts.</p>\n<p>Gobstopper uses two tools for this. Kani checks small Rust functions against every value of their inputs, and Lean proves laws about transcripts of any length.</p>\n<h2 id=\"kani-checks-the-limits-for-every-number\">Kani checks the limits for every number</h2>\n<p>Kani is a model checker for Rust. You write a small proof function that asks for arbitrary values, calls the real code, and asserts what must be true. Kani then checks that assertion for every value those inputs can take.</p>\n<p>Gobstopper's plan limits live in one small Rust module that the normal validation code calls. Here is the plan-size check and its proof, from the source with the coverage checks left out:</p>\n<pre><code class=\"language-rust\">pub const MAX_EDITS: usize = 64;\npub const MAX_ITEMS: usize = 100_000;\n\npub fn plan_bounds(edits: usize, items: usize) -&gt; bool {\n    edits &lt;= MAX_EDITS &amp;&amp; items &lt;= MAX_ITEMS\n}\n\n#[kani::proof]\nfn production_plan_bounds() {\n    let edits: usize = kani::any();\n    let items: usize = kani::any();\n    let valid = plan_bounds(edits, items);\n    assert_eq!(valid, edits &lt; 65 &amp;&amp; items &lt; 100_001, &quot;exact production plan bounds&quot;);\n}\n</code></pre>\n<p>The proofs sit beside the functions Gobstopper runs, and they do not swap in smaller limits to make the check easier. Among the laws they establish, each over every value of its numeric inputs:</p>\n<ul>\n<li>A plan with at most 64 edits over at most 100,000 transcript items passes, and anything larger is refused.</li>\n<li>Summary text added to a plan stays at or under 32,768 bytes, and an addition that would overflow is refused instead of wrapping.</li>\n<li>A token estimate is the byte count divided by four and rounded up, never zero, with no overflow at the largest byte count.</li>\n<li>Adding token counts saturates at the maximum value instead of wrapping around, so a running total never shrinks.</li>\n<li>The savings estimated for replacing one output, after subtracting the stub's own cost, are never larger than that output's token estimate, and an output with no bytes or a zero estimate saves nothing.</li>\n<li>The count of records outside the protected recent tail is exactly the total minus the tail, or zero when the tail covers everything, with no wraparound for any two inputs.</li>\n<li>Adding an edit to a plan either succeeds and keeps the plan valid, or fails and leaves the plan's state unchanged. A plan holds at most one summary, and a provider or cache control edit must be the plan's only edit.</li>\n</ul>\n<p>Two groups of proofs cover fixed sizes only. The edit-sequence proof checks every sequence of four edits, each of any of the four edit kinds, while the one-step proof covers the real 64-edit limit, and the sorted lookup of record positions is checked for lists of length 0, 1, 2, and 4. Everywhere else the inputs are single numbers, and Kani covers their whole range.</p>\n<p>To show that the proof can fail, the check plants a bug. It copies the crate, changes <code>edits &lt;= MAX_EDITS</code> to <code>edits &lt; MAX_EDITS</code> in that copy, and reruns the proof. The run passes only if that one assertion fails. The repository's CI runs the positive proofs and this planted bug on every pull request and every push to main.</p>\n<h2 id=\"lean-proves-what-masking-does-to-a-transcript\">Lean proves what masking does to a transcript</h2>\n<p>Lean is a proof assistant. It checks proofs about definitions, for lists and numbers of any size, down to a small trusted kernel.</p>\n<p>Gobstopper's Lean model treats a transcript as a list of records. Each record has a stable ID, flags for whether it is live, eligible, and protected, some content, and an optional tool call or tool result. Masking a set of selected IDs replaces the content of each selected, live, eligible, unprotected record with a stub and leaves every other record alone. Two of the laws, shown without their proofs, look like this:</p>\n<pre><code class=\"language-lean\">-- Masking twice with the same selection is the same as masking once.\ntheorem idempotence (selected : List Nat) (records : List Record) :\n    mask selected (mask selected records) = mask selected records\n\n-- A selection that is refused leaves the transcript exactly as it was.\ntheorem rejection_identity (records : List Record) (selected : List Nat)\n    (h : admitted records selected = false) : execute records selected = records\n</code></pre>\n<p>The model's 27 theorems include these laws, for transcripts of any length:</p>\n<ul>\n<li>Masking keeps the transcript's length, the order of records, and every record's ID.</li>\n<li>Protected records and records that are not live keep their content.</li>\n<li>Every selection the rules accept names a known record that is live, eligible, and unprotected.</li>\n<li>The sequence of tool calls and tool results is unchanged, so if every result followed its one call before, it still does.</li>\n<li>With the records' flags held fixed, masking with one selection and then another equals masking once with both.</li>\n<li>Appending a summary record with a fresh ID keeps every earlier record in place, keeps tool links valid, and keeps IDs unique.</li>\n</ul>\n<p>The checker rebuilds the proofs from scratch, replays them in a fresh Lean kernel, and allows only Lean's two standard axioms.</p>\n<h2 id=\"tying-the-lean-model-to-the-shipped-rust\">Tying the Lean model to the shipped Rust</h2>\n<p>A proof about a model helps only if the model matches the program, and Gobstopper checks that match with tests. A Lean program runs the proved operations on 20 synthetic cases and writes the expected results to a file. A Rust test then builds synthetic transcript files in the Codex, Claude Code, and Devin formats, runs the same 23 steps through Gobstopper's actual validation and transform code for each provider, and compares the output record by record: 12 accepted steps, including 3 that add a summary, and 11 refused.</p>\n<p>Two planted bugs check that the comparison can fail from either side. One changes the Lean side to write a masked record's value as 1 instead of 0. The other changes the Codex writer in Rust so it replaces nothing. Each must make the comparison fail on the expected record. CI runs the Lean proofs, the correspondence test, and both planted bugs on every pull request and push to main.</p>\n<h2 id=\"where-the-repository-records-each-proofs-scope\">Where the repository records each proof's scope</h2>\n<p>Gobstopper's public repository keeps a claims register that states the scope and exclusions of each check. It lists both the Kani and Lean claims with the status <code>bounded_check</code>, meaning each holds for its stated inputs and limits and no further. The records of passing runs for both are dated September 24, 2026, and tie each run to hashes of the exact source files and tool binaries it checked. A change to any of those files voids the record until the checks pass again; on September 24 the recorded source hashes still matched the main branch.</p>\n<h2 id=\"what-the-proofs-do-not-cover\">What the proofs do not cover</h2>\n<p>The proofs do not show that a session stays under any context budget: Gobstopper's token counts are estimates at four bytes per token, not the provider's tokenizer or bill. The Lean laws are about the structure of an edit; they do not show that a compacted transcript can be turned back into the original, or that a lossy stub keeps what a later task needs. Getting the original back relies on Gobstopper's archive, which these proofs do not cover. The Rust correspondence covers 20 small, valid, linear synthetic histories; arbitrary provider files, branched sessions, and a live resume fall outside it. Parsing, file writes, sorting, allocation, the compiler, and the operating system are trusted rather than proved, and a Kani run proves its result for the platform it ran on. Gobstopper's own ledger records whole-system correctness as an objective with no complete proof.</p>\n",
    "toc": [
      {
        "href": "#code-that-is-right-until-the-long-session",
        "label": "Code that is right until the long session"
      },
      {
        "href": "#what-changes-when-the-edge-cases-are-ruled-out",
        "label": "What changes when the edge cases are ruled out"
      },
      {
        "href": "#kani-checks-the-limits-for-every-number",
        "label": "Kani checks the limits for every number"
      },
      {
        "href": "#lean-proves-what-masking-does-to-a-transcript",
        "label": "Lean proves what masking does to a transcript"
      },
      {
        "href": "#tying-the-lean-model-to-the-shipped-rust",
        "label": "Tying the Lean model to the shipped Rust"
      },
      {
        "href": "#where-the-repository-records-each-proofs-scope",
        "label": "Where the repository records each proof's scope"
      },
      {
        "href": "#what-the-proofs-do-not-cover",
        "label": "What the proofs do not cover"
      }
    ]
  },
  "vault-models-that-fail-on-purpose": {
    "html": "<p>Two programs share a folder. One is writing a new file into it, piece by piece. The other is tidying up, deleting anything it thinks nobody needs. Each one works when it runs alone. Run them at the same moment, and the tidier can decide a half-written file is junk, wait for the writer to finish and register it, and then delete the pieces it already marked. The writer reports success. The file is gone.</p>\n<p>Gobstopper's archive has exactly this shape. Before Gobstopper writes a compacted copy of a Claude Code or Codex session, it saves the original transcript into a local archive, and a cleanup command can later remove old snapshots. To trust that archive, you need to know what happens when the machine dies halfway through a save, or when cleanup runs during one. Gobstopper checks the archive's design with a program that tries a crash at every step, in every order, within a small fixed world, and tests the actual code with random sequences of edits and restores. Neither check covers a power cut that scrambles unsynced disk writes.</p>\n<p><strong>Status:</strong> Latest release: {{release.version}}. The checks in this post run against the current source on the main branch. The TLA+ models were added after that release, so it does not include them.</p>\n<h2 id=\"storage-bugs-show-up-weeks-after-the-cause\">Storage bugs show up weeks after the cause</h2>\n<p>Plenty of software now ships as vibe-coded slop: code a model produced quickly that looks finished and breaks on the second use. Underneath a lot of it sits a fragile foundation, a storage layer nobody checked beyond the happy path. Storage bugs are the worst kind to find late, because the symptom shows up weeks after the cause. A typical one appears only if you start a save, lose power, reopen, and then run cleanup before the next save. Nobody types that sequence into a test by hand.</p>\n<p>A save that is interrupted is ordinary. Laptops sleep, terminals close, processes get killed. What matters is what the half-finished save leaves behind, and whether some other part of the program can mistake that leftover state for garbage, or for success.</p>\n<h2 id=\"a-search-over-every-ordering-checked-by-planted-bugs\">A search over every ordering, checked by planted bugs</h2>\n<p>You can write down the few things that must always be true about the archive, such as &quot;a snapshot the index lists still has all its pieces,&quot; and then have a tool try every possible ordering of saves, reads, cleanups and crashes in a small world, stopping if any ordering breaks a rule. When that search finishes clean, a whole class of failure is ruled out for that design at that size, including orderings no person would think to test.</p>\n<p>The catch is that a search can pass for a bad reason. A model that never lets anything happen will never break a rule. So each check comes with deliberately broken versions: copies of the design with one safety rule switched off, which must fail with the specific loss that rule exists to prevent. If a broken copy passes, the check is not looking at what it claims to.</p>\n<h2 id=\"how-gobstopper-saves-a-snapshot\">How Gobstopper saves a snapshot</h2>\n<p>The archive stores a transcript as content-addressed chunks, a per-snapshot list of which chunks it needs, and an index of snapshots. A save runs in a fixed order:</p>\n<ol>\n<li>Write the chunks.</li>\n<li>Write the snapshot's chunk list.</li>\n<li>Read it back and check the snapshot reconstructs.</li>\n<li>Add it to the index.</li>\n<li>When the save belongs to a compaction, record a pin, a note that an operation depends on this snapshot, so cleanup must keep it.</li>\n</ol>\n<p>Saves and reads take a shared lock on the archive folder. Cleanup takes an exclusive lock, so it cannot run while any save or read holds the shared one. Cleanup keeps the snapshots its retention setting selects plus every pinned one, and deletes chunks that nothing kept refers to.</p>\n<h2 id=\"the-rules-the-vault-model-checks\">The rules the vault model checks</h2>\n<p>The vault model is written in TLA+, a language for describing a system as states and the steps between them. The TLC model checker then walks every reachable state. The model has two writers, one reader, one cleanup pass, three snapshots and four chunks, with one chunk shared between the old snapshot and both new ones. A writer can crash after any step. The reader and cleanup can crash too, and a crash releases the lock. Cleanup may choose to keep any subset of snapshots, including none, so the check covers every retention setting at once.</p>\n<p>The rules, in plain words:</p>\n<ul>\n<li>Every snapshot in the index still has its chunk list and every chunk.</li>\n<li>Every pinned snapshot can still be rebuilt, even after the writer that pinned it has crashed.</li>\n<li>A reader that has started rebuilding a snapshot cannot have it deleted underneath it.</li>\n<li>Cleanup never holds its exclusive lock while a writer or reader is active.</li>\n</ul>\n<p>The first rule, as the model states it (a manifest is a snapshot's chunk list, and <code>Parts(s)</code> is the set of chunks it names):</p>\n<pre><code class=\"language-tla\">DataValid(s) == s \\in manifests /\\ Parts(s) \\subseteq objects\nIndexedData  == \\A s \\in index : DataValid(s)\n</code></pre>\n<p>In the run recorded on 24 September 2026, the safe configuration explored 25,810 distinct states and finished with nothing left to explore, so every reachable state satisfied all four rules.</p>\n<h2 id=\"two-broken-copies-that-must-fail\">Two broken copies that must fail</h2>\n<p>The first broken copy removes the lock. The checker must then find a way to break the index rule, and it does: a writer stores its chunks, cleanup marks them as unreferenced because no chunk list names them yet, the writer finishes and indexes its snapshot, and cleanup deletes the chunks it marked. The index now lists a snapshot that cannot be rebuilt. The model gives cleanup an atomic check that the index has not changed before it acts, and the race still gets through. That is why saves and cleanup share a lock instead of relying on a last-moment comparison.</p>\n<p>The second broken copy keeps the lock but lets cleanup ignore pins. The checker must break the pin rule: a writer pins its snapshot, crashes, releases its lock, and cleanup collects the data that recovery would have needed.</p>\n<p>Two more configurations check the opposite direction. One must reach a state where both writers, the reader and cleanup all finish. Another must reach a crashed writer whose pin still protects its snapshot. They show the model permits both finished and interrupted work, so the rules held while things actually happened.</p>\n<p>The test runner does not accept any failure as the expected one. Each broken copy must fail on its named rule, with a complete log. A timeout, a parse error or a different broken rule counts as a failed check.</p>\n<h2 id=\"publishing-a-copy-and-recovering-after-a-crash\">Publishing a copy and recovering after a crash</h2>\n<p>A second model covers the part after the snapshot: writing the compacted copy and recovering if the process dies. It has one writer, one reader, up to two cleanup passes and one restart, and it includes torn index and record states, a conflicting file at the destination and a failed delete. Its rules:</p>\n<ul>\n<li>The compacted copy appears only after a durable record says an operation is in progress.</li>\n<li>An operation is marked complete only after the copy is visible and the folder has been synced.</li>\n<li>Damaged index or operation state stops cleanup entirely.</li>\n<li>A snapshot's chunk list never outlives its chunks, even when a delete fails partway.</li>\n</ul>\n<p>Four broken copies each switch one of those off and must fail on it. A fifth configuration must reach a completed operation after an interruption and a restart. Recovery never reruns the compaction; it checks the stored record and either confirms the exact output or refuses. In the same 24 September run, this model explored 28,082 distinct states and finished.</p>\n<h2 id=\"the-same-method-for-native-compaction-requests\">The same method for native compaction requests</h2>\n<p>Gobstopper's watch mode has a separate model for asking a provider to compact a live session. That path is turned off in released builds, and the model exists so the design is checked before it is turned on. Its main rule is to write down &quot;about to send&quot; before sending. A crash between that note and the actual request leaves the same record as a request whose answer was lost, so the only safe reading is &quot;unknown.&quot; The model checks that an unknown outcome is never cleared by a timer or a configuration change, that each operation is sent at most once, that an acknowledgement alone never counts as success, that the recovery snapshot is pinned before the operation is recorded, that uncertainty never triggers an automatic fallback to editing files, and that an unknown outcome is settled only with retained evidence that exactly matches the request.</p>\n<p>Five broken copies must each fail on one of those rules, and six more configurations must reach real outcomes, including a crash that leaves a request in the &quot;sent, outcome unknown&quot; state. The safe configuration covers two watchers and two operations. It explored 32,251 distinct states in the 24 September run.</p>\n<h2 id=\"random-edits-against-the-real-code\">Random edits against the real code</h2>\n<p>The models run none of Gobstopper's Rust. To test the code itself, Gobstopper uses Hegel, a property-testing library that draws random inputs, runs a random sequence of commands, and shrinks any failure to a short case a person can read.</p>\n<p>Each test case generates a random Claude Code or Codex transcript, with tool calls and results, side branches, bookkeeping records and awkward text, then runs between one and twelve random commands against the production code: shorten some records, insert a summary, snapshot and restore from the archive, or append new records the way a provider would. After every command it checks that the transcript still passes Gobstopper's structural checks, and after each shortening that the fields linking records together have not moved. The shape of the loop, simplified from the test file:</p>\n<pre><code class=\"language-rust\">// Simplified sketch; the real test inlines each step.\nfor _ in 0..tc.draw(integers().min_value(1).max_value(12)) {\n    match tc.draw(integers::&lt;u8&gt;().max_value(3)) {\n        0 =&gt; shorten_random_records(&amp;tc, &amp;path),\n        1 =&gt; insert_summary(&amp;tc, &amp;path),\n        2 =&gt; snapshot_edit_and_restore(&amp;tc, &amp;path, &amp;archive),\n        _ =&gt; append_like_the_provider(&amp;tc, &amp;path),\n    }\n    assert_passes_structural_checks(&amp;path);\n}\n</code></pre>\n<p>The restore step asserts that a snapshot comes back byte for byte, into a new file, without touching the original. A separate property saves the same transcript twice and checks that identical content gets the same identity and restores exactly. Each property runs 64 generated cases. These tests edit files the test itself created; they are not permission to edit a provider's own session files, and released builds do not.</p>\n<p>Two regression tests in the same file are marked as coming from shrunk failures. In one, shortening a Codex output that was already under the size floor produced a stub larger than the original. In the other, shortening the same records twice changed the bytes a second time. Both now have fixed tests alongside the random ones.</p>\n<p>Process crashes in the real code get their own tests. The storage tests kill child processes with <code>SIGKILL</code> at declared points, then check that the lock is released and a fresh process recovers.</p>\n<h2 id=\"keeping-the-claims-tied-to-the-files\">Keeping the claims tied to the files</h2>\n<p>Gobstopper keeps a machine-readable ledger of what each check covers, its limits and what it excludes. Each model run produces a record with the SHA-256 of every model, configuration and runner file it used, plus hashes of the Java and TLC programs that ran it. A script in CI reads the ledger and fails if a recorded run did not pass, or if any file it lists has changed since the run. Editing a model without rerunning it breaks the build instead of leaving an out-of-date claim in place. A separate CI job reruns both models and all their broken copies on every pull request and push to main.</p>\n<h2 id=\"limits-of-the-models-and-tests\">Limits of the models and tests</h2>\n<p>Gobstopper as a whole is not formally verified. The models check a finite design with a small, fixed number of writers, readers and cleanup passes. They do not prove anything about larger counts, about eventual progress, or about the Rust code itself; the match between model steps and Rust functions is a reviewed table, not a proof. Every save in the models is atomic and durable. A power cut that loses or reorders unsynced writes, a disk that fills, a network filesystem, Windows locking, and another program that writes into the archive folder without taking the lock are all outside what they check. The Hegel tests cover 64 random cases per property on synthetic transcripts. The kill tests stop processes; they do not cut power to hardware.</p>\n",
    "toc": [
      {
        "href": "#storage-bugs-show-up-weeks-after-the-cause",
        "label": "Storage bugs show up weeks after the cause"
      },
      {
        "href": "#a-search-over-every-ordering-checked-by-planted-bugs",
        "label": "A search over every ordering, checked by planted bugs"
      },
      {
        "href": "#how-gobstopper-saves-a-snapshot",
        "label": "How Gobstopper saves a snapshot"
      },
      {
        "href": "#the-rules-the-vault-model-checks",
        "label": "The rules the vault model checks"
      },
      {
        "href": "#two-broken-copies-that-must-fail",
        "label": "Two broken copies that must fail"
      },
      {
        "href": "#publishing-a-copy-and-recovering-after-a-crash",
        "label": "Publishing a copy and recovering after a crash"
      },
      {
        "href": "#the-same-method-for-native-compaction-requests",
        "label": "The same method for native compaction requests"
      },
      {
        "href": "#random-edits-against-the-real-code",
        "label": "Random edits against the real code"
      },
      {
        "href": "#keeping-the-claims-tied-to-the-files",
        "label": "Keeping the claims tied to the files"
      },
      {
        "href": "#limits-of-the-models-and-tests",
        "label": "Limits of the models and tests"
      }
    ]
  }
} as const;
