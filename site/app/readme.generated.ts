// Generated from ../README.md by scripts/sync-readme.ts. Do not edit.
export const readmeTitle = "Gobstopper";
export const readmeLead = "Gobstopper is a free, open-source command-line tool that makes long coding sessions smaller. Its main tool, `gobstopper proxy`, runs on your machine between a coding agent and its model provider. When a request passes a token threshold, the proxy replaces the older turns with one mechanical summary and sends the newest turns word for word, so the provider sees a smaller context and the agent's own auto-compaction does not reach its trigger.";
export const readmeHtml = "<h1 id=\"gobstopper\">Gobstopper</h1>\n<p>Gobstopper is a free, open-source command-line tool that makes long coding\nsessions smaller. Its main tool, <code>gobstopper proxy</code>, runs on your machine\nbetween a coding agent and its model provider. When a request passes a token\nthreshold, the proxy replaces the older turns with one mechanical summary and\nsends the newest turns word for word, so the provider sees a smaller context\nand the agent's own auto-compaction does not reach its trigger.</p>\n<p>The summary rule comes from CliffCompaction, an open-source proxy described\nin a September 2026 paper by Trang Nguyen, Eulrang Cho, Bingqing Chen, and\nTim Dettmers. <code>gobstopper proxy</code> is a Rust port of it for the three dialects\ncoding agents use: Anthropic Messages (Claude Code, opencode, Crush), OpenAI\nResponses (Codex), and OpenAI Chat Completions (opencode, Crush, Aider,\nGoose, and other OpenAI-compatible clients). Run <code>gobstopper proxy run -- claude</code> to try it on one session, or see\n<a href=\"#compact-live-coding-agent-requests\">Compact live coding-agent requests</a>.</p>\n<p>Gobstopper also works on saved Claude Code and Codex session files. Preview a\ncompaction at a context size you choose, prepare a separate smaller copy, and\nkeep the original byte for byte in a local vault. It archives the exact source\nand candidate bytes and checks supported structural properties and protected\nrecent output.</p>\n<p>Use <code>gobstopper watch --dry-run</code> to inspect threshold decisions, or prepare a\ncopy with a file strategy. Released CLI builds cannot ask providers to compact,\neven when <code>auto_compact_closed</code> is enabled. Direct provider-store and in-place\nrewrites are disabled. Copy preparation preserves the source; resuming a copy\nwith a live provider requires separate compatibility testing. See the\n<a href=\"https://github.com/hraness/gobstopper/blob/main/docs/assurance/qualification.json\">provider support status</a> and\n<a href=\"https://github.com/hraness/gobstopper/blob/main/docs/assurance/operations.md\">recovery runbook</a>. Plugins can add strategies\nand providers when you explicitly trust them.</p>\n<h2 id=\"why\">Why</h2>\n<p>A coding agent resends its whole history with every request. In a long\nsession that history fills with old file reads and command output the next\nstep rarely needs, and every request pays to send it again. When the history\nnears the model's window, the agent asks a model to summarize it. That call\nis itself a large request, the summary can leave out an exact error or\nconstraint, and each later summary summarizes the one before.</p>\n<p><code>gobstopper proxy</code> keeps each request under a threshold you choose, without\na model call:</p>\n<ul>\n<li><strong>Recent work stays word for word.</strong> The system prompt, the first task,\nand the newest turns (at least three, and more while they fit its tail\nbudget) are sent unchanged. Older turns become one summary that keeps human\nand assistant text, keeps tool results of at most 500 characters, and\nreduces each tool call to a one-line signature. Longer tool results are\ndropped, because the agent can read the file or rerun the command.</li>\n<li><strong>A summary is never summarized.</strong> Each compaction starts again from the\noriginal history the client resends and discards the previous summary.</li>\n<li><strong>The prompt cache keeps matching.</strong> Between compactions, every request\nreuses the same compacted prefix byte for byte, so the provider's prompt\ncache stays valid until the next compaction.</li>\n<li><strong>The agent's own compaction stays idle.</strong> The provider reports the\ncompacted size, so Claude Code or Codex does not reach its auto-compaction\ntrigger, and your transcript keeps the full history.</li>\n<li><strong>A failure sends the original request.</strong> If the body cannot be parsed,\nthe engine fails, or the provider rejects the rewritten request for a\nreason other than length, the proxy forwards the client's original bytes.\nA length rejection gets a further compaction and a retry.</li>\n</ul>\n<h3 id=\"what-cliffcompactions-authors-report\">What CliffCompaction's authors report</h3>\n<p>The <a href=\"https://arxiv.org/abs/2609.26779\">CliffCompaction paper</a> reports up to\n50% lower cost at a bounded context, with Terminal-Bench 2.0 scores held or\nimproved, on the Kimi K2.6 and GLM 5.1 models its authors tested. In one run\nthrough Claude Code (GLM 5.3 Flash on Terminal-Bench 2.1, at about 45,000\ntokens of mean peak context), the rule scored 76.69%, against 70.97% for\nClaude Code's own auto-compaction and 73.03% for its default 200,000-token\nsetting. The paper's costs come from a model of perfect prompt caching, not\nmetered bills. The authors also report that the benefit depends on the agent\nand the task, and that it matters only for medium-to-long tasks.</p>\n<p>These are the authors' measurements of their own proxy. <code>gobstopper proxy</code>\nshares its summary rule and departs from it in five places, listed in <a href=\"#how-gobstopper-compares-with-cliffcompaction\">How\nGobstopper compares with\nCliffCompaction</a>. Gobstopper\nhas not rerun those benchmarks, and it has not measured task quality or\nbilled cost under the proxy.</p>\n<h3 id=\"what-gobstopper-has-measured\">What Gobstopper has measured</h3>\n<p>Replays of nine recorded sessions through the proxy engine, built from\n<code>main</code> on September 25, 2026, kept Claude Code sessions whose requests peaked at 273k to 652k\nestimated tokens at or under about 127k; see <a href=\"#compact-live-coding-agent-requests\">Compact live coding-agent\nrequests</a>. On September 26, 2026, one\nMac sent about 77 minutes of Claude Code traffic through <code>gobstopper proxy serve</code> from the v0.4.1 release at its defaults. Of 3,136 requests, most were\nsmall (median about 6,300 estimated tokens). 89 passed the 128,000-token\nthreshold; the proxy sent 78 of them smaller, 12.2 million estimated tokens\nin total down to 7.5 million (38% less). The other 11 went out unchanged.\nv0.4.1 does not record why; the likely cause is a threshold raised by a\nlarge verbatim head (see <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/proxy.md#how-it-works\">How it works</a>), and\nthe current build records the threshold applied to each request. There were no provider errors and no\nlength retries. These are estimates from one machine over one afternoon, not\nbilled tokens or task results.</p>\n<h3 id=\"saved-sessions\">Saved sessions</h3>\n<p>For session files, Gobstopper lets you check the tradeoff before you commit\nto it. You can preview a compaction, compare strategies on the same frozen\nbytes, keep the exact source in a local vault, and recover a specific\narchived record when a copy leaves it out. The built-in strategies use local\nrules and need no model. Optional model scorers change what gets selected;\nthey do not skip the snapshot or the verification step. A running session's\ncontext belongs to the provider process that loaded it, so file compaction\nprepares a separate copy. The <a href=\"https://gobstopper.sh/benchmarks\">published\nstudies</a> report context reduction,\nretention, no-op cases, and limitations separately.</p>\n<p>Gobstopper keeps the exact source in a local vault before any compaction changes it, so a compaction is a recorded edit you can recover from rather than a silent loss: the design every Hraness project shares. <a href=\"https://hraness.com/writing/the-thread-through-hraness\">The thread through hraness</a> follows that design across the projects, and the <a href=\"https://algal.computer/docs/vision/\">ALGAL vision</a> states the bet behind it.</p>\n<h2 id=\"compact-live-coding-agent-requests\">Compact live coding-agent requests</h2>\n<p><code>gobstopper proxy</code> is a local HTTP proxy that sits between a coding agent\nand its model provider. It has shipped in tagged releases since v0.3.1 and\ngained the Chat Completions dialect in v0.4.0; the tail budget and the\nseparate 1M-token threshold described here are in the current <code>main</code> source\nbuild and not yet in a release. Each time the\nclient resends its history, the proxy estimates the request size. Past the\nthreshold (128,000 tokens by default), it sends the system prompt and the\nfirst task verbatim, one mechanical summary of the older turns, and the\nnewest turns verbatim: at least three, plus older whole turns while the\nsummary and the kept turns fit in 40% of the room left under the threshold\nafter the system prompt and the first task. The provider then reports the\ncompacted size back to the client, so the client's own auto-compaction does\nnot reach its trigger. The summary rule is CliffCompaction's; see <a href=\"#how-gobstopper-compares-with-cliffcompaction\">How\nGobstopper compares with\nCliffCompaction</a>.</p>\n<p>It speaks the three dialects coding agents use:</p>\n<table>\n<thead>\n<tr><th>Agent</th><th align=\"right\">Dialect</th><th align=\"right\">How to point it at the proxy</th></tr>\n</thead>\n<tbody>\n<tr><td>Claude Code</td><td align=\"right\">Anthropic Messages</td><td align=\"right\"><code>export ANTHROPIC_BASE_URL=http://127.0.0.1:8260</code></td></tr>\n<tr><td>Codex</td><td align=\"right\">OpenAI Responses</td><td align=\"right\"><code>model_providers</code> block in <code>~/.codex/config.toml</code></td></tr>\n<tr><td>opencode</td><td align=\"right\">Anthropic Messages or Chat Completions</td><td align=\"right\"><code>provider.&lt;id&gt;.options.baseURL</code> → <code>http://127.0.0.1:8260/v1</code></td></tr>\n<tr><td>Crush</td><td align=\"right\">Anthropic Messages or Chat Completions</td><td align=\"right\"><code>providers.&lt;id&gt;.base_url</code> → <code>http://127.0.0.1:8260/v1</code></td></tr>\n<tr><td>Aider</td><td align=\"right\">Chat Completions</td><td align=\"right\"><code>aider --openai-api-base http://127.0.0.1:8260/v1</code></td></tr>\n<tr><td>Goose</td><td align=\"right\">Chat Completions</td><td align=\"right\"><code>OPENAI_HOST=http://127.0.0.1:8260</code></td></tr>\n</tbody>\n</table>\n<p>The setup for each agent is in <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/proxy.md\">docs/proxy.md</a>. Any other\nOpenAI-compatible client that posts to <code>{base}/chat/completions</code> works the\nsame way. Claude Code and Codex routing is live-checked; the Chat\nCompletions dialect is contract-tested against synthetic histories and has\nnot yet been qualified against a live opencode, Crush, Aider, or Goose\nsession.</p>\n<p>The summary keeps human and assistant text, keeps tool results of at most 500\ncharacters, and reduces each tool call to a one-line signature; longer tool\nresults are dropped because the agent can read the file or rerun the command.\nThe next compaction starts again from the history the client resends and\ndiscards the previous summary. Between compactions, requests reuse the same\ncompacted prefix, so the provider's prompt cache can match it.</p>\n<p>The kept turns carry the files and command output the agent read most\nrecently, which the summary drops once they pass 500 characters. Unless the\nnewest <code>--keep-recent</code> turns alone need more, the summary and the kept turns\nfill at most 40% of that room, and the other 60% is left for new turns\nbefore the next compaction. <code>--keep-tail-percent</code> sets the share, from 0 to\n60, and <code>0</code> keeps exactly <code>--keep-recent</code> turns. Anthropic Messages requests\nthat declare a 1M-token context window use a separate threshold,\n<code>--threshold-1m</code>: 256,000 estimated tokens by default, or <code>--threshold</code> if\nthat is higher. The proxy reads the window from the <code>anthropic-beta</code> header,\nwhere Claude Code sends a token starting with <code>context-1m</code> for a model such\nas <code>opus[1m]</code>, and never from the model name. Setting <code>--threshold-1m</code> equal\nto <code>--threshold</code> applies one threshold to every request. Keep each threshold\nbelow the point where the client compacts on its own, including any <code>claude --autocompact</code> value.</p>\n<pre><code class=\"language-sh\">gobstopper proxy run -- claude            # one session through a temporary proxy\ngobstopper proxy serve                    # background proxy on http://127.0.0.1:8260\nexport ANTHROPIC_BASE_URL=http://127.0.0.1:8260\ngobstopper proxy replay &lt;session&gt;         # what the proxy would have sent; calls no provider\ngobstopper proxy status                   # counters and estimated-token totals, this run and all time\n</code></pre>\n<p>See <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/proxy.md\">docs/proxy.md</a> for per-agent setup (Claude Code, Codex,\nopencode, Crush, Aider, Goose), a macOS LaunchAgent, and every setting.\nFlags: <code>--threshold</code> (keep it below the client's auto-compaction point),\n<code>--threshold-1m</code>, <code>--keep-recent</code>, <code>--keep-tail-percent</code>,\n<code>--result-max-chars</code>, <code>--drop-thinking</code>, <code>--shadow</code> (log what would change\nand forward everything unchanged), and <code>--strict</code>.</p>\n<ul>\n<li>The proxy listens on 127.0.0.1 and refuses requests addressed to other\nhost names. It forwards through the system <code>curl</code> (8.3 or later) and hands\nrequest headers, which carry your API key or sign-in token, to curl through\nits environment instead of its command line. Logs contain sizes and counts,\nnever request or response text.</li>\n<li>On any failure it forwards the client's original bytes: an unparseable or\ncompressed body, an internal error, or a provider that rejects the\nrewritten request for a reason other than length. When the provider rejects\na request for length, the proxy compacts further and retries.</li>\n<li>Transcript files are not changed. The client keeps its full history, so\nresume works as before, and Claude Code and Codex histories still feed the\nfile commands below.</li>\n<li>Sizes are estimates at four characters per token, with images priced by\ntheir dimensions. A history the client already compacted itself can start\nwith a long head that the proxy keeps verbatim; the threshold then rises to\nthat head plus half the request's threshold.</li>\n</ul>\n<p>On September 25, 2026, <code>gobstopper proxy replay</code> with three kept turns, the\ndefault on that date, over nine recorded sessions on one Mac kept six Claude\nCode sessions, whose recorded requests peaked at 273k to 652k estimated\ntokens, at or under about 127k, and one Codex session that peaked at 242k\nunder about 127k. Two Codex sessions that Codex had already compacted itself\nbegan with heads near 160k and stayed under about 243k. No replayed request\nwas left with an unpaired tool call. These are estimates over recorded\nhistories, not billed tokens or task results.</p>\n<h2 id=\"recoverable-history\">Recoverable history</h2>\n<p>Before publishing a Claude Code or Codex copy, Gobstopper stores the exact\nsource and candidate bytes in a content-addressed vault\n(<code>~/.local/share/gobstopper/vault/</code>). Snapshots use deduplicated 1 MiB chunks,\nso appended versions reuse\nunchanged prefix storage without creating one filesystem object per JSONL\nrecord.</p>\n<p><code>gobstopper recall --query &lt;q&gt;</code> searches the state cards in every archived\nsnapshot, ranks matches by relevance to the query, and returns the high-level\nstate of the matching turns. An agent does not need to remember session IDs:\nit can ask for the last time it worked on a file, a goal, or a decision and\nget a ranked summary with a snapshot SHA to pass to <code>show</code> or <code>diff</code>.</p>\n<h3 id=\"recover-a-specific-detail\">Recover a specific detail</h3>\n<p>When a state card omits an exact error, identifier, or tool result, search\none verified snapshot and read only the matching record:</p>\n<pre><code class=\"language-sh\">gobstopper search-snapshot &lt;full-snapshot-sha&gt; --query 'exact error text' --json\ngobstopper read-snapshot &lt;full-snapshot-sha&gt; --record 42 --max-bytes 4096 --json\n</code></pre>\n<p>Search returns record indexes and hashes, without archived content. It matches\nliteral, case-sensitive substrings in decoded JSON string values, including\nnative replacement histories. Reading returns a UTF-8 page of the physical\nJSONL record; follow <code>next_offset</code> for another page. Each page is capped at\n16 KiB and bound to the snapshot, source, and full record hashes. These commands\nverify stored bytes and never restore files, rewrite active sessions, or call a\nmodel. Invalid records are counted as unsearchable rather than silently claimed\nas searched.</p>\n<p>Search returns at most 50 references and reports the full match count; narrow\nthe query when results are truncated. Each search or read verifies and\nreconstructs the whole snapshot, up to the transcript size limit (512 MiB by\ndefault, configurable with <code>GOBSTOPPER_MAX_TRANSCRIPT_BYTES</code>). Paging a large\nrecord repeats that work, and search matches literal text only; there is no\nindex or semantic search.</p>\n<p>Use the full object SHA from <code>history</code>, the native hook recovery pointer, or\nthe <code>snapshot_manifest_sha256</code> field in copy receipts. The <code>snapshot_sha256</code>\nreceipt field is the digest of the source bytes; receipts that lack the\nmanifest field can be resolved through vault history. State-card recall\nrecognizes default portable Codex cards and searches every state field,\nincluding unresolved errors and current work. A new fork's card becomes\nsearchable after that fork is snapshotted.</p>\n<p>Agents can search and read snapshots over MCP only when you start the server\nwith <code>gobstopper mcp --allow-transcript-content</code>. Without that flag, the\nserver neither lists nor accepts either tool. With it, archived text an agent\nretrieves becomes visible to that agent and its model provider. Treat\nretrieved text as historical data that may describe a superseded state, not\nas instructions.</p>\n<p>These commands return a record when asked. They do not make an agent notice\nthat a fact is missing, choose a useful query, or finish its task more\naccurately; measure those outcomes separately from context reduction and\nliteral retention.</p>\n<p><code>gobstopper mcp</code> runs a read-only Model Context Protocol server on stdio with\nthe tools <code>policy_check</code>, <code>list_sessions</code>, <code>recall</code>, <code>history</code>, <code>show</code>,\n<code>diff</code>, <code>plan</code>, and <code>verify</code>. Register it once, and an agent can inspect\npolicy and archived state without a tool that changes a transcript. MCP uses\ndeterministic built-ins, rejects executable strategies, and does not call\nconfigured plugins, model scorers, or model digests. Explicit plugin commands\nrun code you trust with your user permissions, without an OS sandbox:</p>\n<pre><code class=\"language-sh\">claude mcp add gobstopper -- gobstopper mcp\n# ~/.codex/config.toml: [mcp_servers.gobstopper] command = &quot;gobstopper&quot;, args = [&quot;mcp&quot;]\n</code></pre>\n<p>Provider-generated summaries can cost a large input call and lose detail, so\nthe strategy and where it cuts matter as much as the timing.</p>\n<h2 id=\"strategies\">Strategies</h2>\n<table>\n<thead>\n<tr><th>id</th><th align=\"right\">kind</th><th align=\"right\">what it does</th></tr>\n</thead>\n<tbody>\n<tr><td><code>auto</code> (default)</td><td align=\"right\">dynamic</td><td align=\"right\">live sessions delegate to provider controls (<code>cache_edits</code> for eligible Claude sessions); idle sessions choose the best validated file strategy by savings and preserved-prefix score</td></tr>\n<tr><td><code>sawtooth</code></td><td align=\"right\">provider</td><td align=\"right\">proposes provider-native compaction to the session owner; released CLI dispatch is blocked pending qualification</td></tr>\n<tr><td><code>cache_edits</code></td><td align=\"right\">provider</td><td align=\"right\">emits bounded Claude <code>tool_use_id</code> values for API-layer context editing; never rewrites a transcript</td></tr>\n<tr><td><code>elide</code></td><td align=\"right\">transcript</td><td align=\"right\">stubs stale tool outputs oldest-first until the floor</td></tr>\n<tr><td><code>cliff</code></td><td align=\"right\">transcript</td><td align=\"right\">keeps the head, the newest three assistant steps, and the newest <code>keep_recent_tool_outputs</code> tool results (default 8) byte-for-byte and drops older eligible tool results over 500 bytes; no floor seeking and no state card (see <a href=\"#how-gobstopper-compares-with-cliffcompaction\">CliffCompaction</a>; for running sessions, use <a href=\"#compact-live-coding-agent-requests\"><code>gobstopper proxy</code></a>)</td></tr>\n<tr><td><code>cache_aware</code></td><td align=\"right\">transcript</td><td align=\"right\">elides a tailward stale-output window and injects a bounded state card while preserving the longest practical prefix</td></tr>\n<tr><td><code>compacted</code></td><td align=\"right\">transcript</td><td align=\"right\">elides stale outputs and injects the state card; synthetic Codex <code>compacted</code> records require <code>--experimental-compacted</code></td></tr>\n<tr><td><code>scored</code></td><td align=\"right\">transcript</td><td align=\"right\">ranks candidates with deterministic recency, error, reference, TF-IDF, duplicate, and tool-type signals before elision</td></tr>\n<tr><td><code>dedupe</code></td><td align=\"right\">transcript</td><td align=\"right\">removes older exact duplicate tool payloads using payload SHA-256, not summaries</td></tr>\n<tr><td><code>micro</code></td><td align=\"right\">transcript</td><td align=\"right\">keeps the newest configured outputs per stable tool label and stubs older ones</td></tr>\n<tr><td><code>middle</code></td><td align=\"right\">transcript</td><td align=\"right\">protects both ends of the transcript and elides eligible middle outputs</td></tr>\n<tr><td><code>structured</code></td><td align=\"right\">transcript</td><td align=\"right\">emits a bounded metadata-derived state card; it is not semantic summarization</td></tr>\n<tr><td><code>agentic</code></td><td align=\"right\">extension</td><td align=\"right\">accepts bounded edit proposals from a command or versioned plugin you trust; Gobstopper still validates every edit</td></tr>\n</tbody>\n</table>\n<p>Custom strategies are userspace code: a preset can name a <code>command</code> that\nreceives the normalized transcript as JSON on stdin and returns an edit\nplan on stdout, or install a versioned <code>gobstopper-plugin.json</code> bundle\n(see <code>gobstopper plugin check</code>). A <code>command</code> runs only with\n<code>trusted_legacy_command = true</code>. Gobstopper checks eligible payloads, protected\nrecent output, edit combinations, digest size, and projected token reduction.\nFile candidates must not introduce supported structural findings. These checks\ncover edit structure and size, not semantic preservation or provider acceptance.</p>\n<h2 id=\"install--use\">Install &amp; use</h2>\n<p>This builds the current <code>main</code> branch, which the commands below describe.\nThe proxy is in every release since v0.3.1; the tail budget and the 1M-window\nthreshold are on <code>main</code> only until the next release. Check the <a href=\"https://github.com/hraness/gobstopper/releases\">release\nnotes</a> for what a tagged\nrelease includes.</p>\n<pre><code class=\"language-sh\">cargo install --git https://github.com/hraness/gobstopper gobstopper\n# or from a checkout: cargo build --release\n\ngobstopper proxy run -- claude     # one Claude Code session through the proxy\ngobstopper proxy serve             # background proxy on http://127.0.0.1:8260\ngobstopper proxy status            # requests compacted, estimated tokens saved\n\ngobstopper detect                  # sessions, context sizes, lifetime burn\ngobstopper plan &lt;session&gt;          # what would happen, under which strategy\ngobstopper plan &lt;session&gt; --trigger 100000 --floor 30000    # tune the trade-off\ngobstopper eval &lt;session&gt;          # compare strategies on the same frozen bytes\ngobstopper apply &lt;session&gt; --strategy elide  # Codex/Claude copy; native requests are refused\ngobstopper verify &lt;session&gt;        # supported structural checks (exit 1 on errors)\ngobstopper fork &lt;session&gt;          # clone under a fresh session id + resume cmd\ngobstopper undo &lt;session&gt;          # Codex/Claude: restore a snapshot into a new fork\ngobstopper vault                   # list snapshots in the undo vault\ngobstopper prune                   # preview keeping the newest 10 snapshots per session\ngobstopper install-hooks --output ./hook-candidates.json  # private settings candidates\ngobstopper watch --dry-run         # inspect threshold decisions without preparing copies\ngobstopper watch --dry-run --active-only --once  # bounded recent-session inspection\ngobstopper explain                 # the occupancy model behind the defaults\ngobstopper recall --query &lt;q&gt;      # search state-card digests across all archived sessions\ngobstopper history &lt;session&gt;       # every archived state of one session\ngobstopper diff &lt;sha-a&gt; &lt;sha-b&gt;    # structural comparison of two vault snapshots\ngobstopper bench                   # compare strategies on recently changed sessions\ngobstopper tune &lt;session&gt;          # preview the adaptive trigger/floor for a session\ngobstopper mcp                     # deterministic inspection; executable strategies are rejected\ngobstopper proxy serve             # compact live Claude Code and Codex requests on 127.0.0.1:8260\n</code></pre>\n<h3 id=\"set-up-gobstopper-for-claude-code-and-codex\">Set up Gobstopper for Claude Code and Codex</h3>\n<ol>\n<li>\n<p>Install the binary from <code>main</code> and check it:</p>\n<pre><code class=\"language-sh\">cargo install --git https://github.com/hraness/gobstopper gobstopper\ngobstopper --version\n</code></pre>\n</li>\n<li>\n<p>Register the MCP server with each agent you use:</p>\n<pre><code class=\"language-sh\">claude mcp add -s user gobstopper -- gobstopper mcp\ncodex mcp add gobstopper -- gobstopper mcp\n</code></pre>\n<p>Confirm with <code>claude mcp list</code> or <code>codex mcp list</code>.\nAdd <code>--allow-transcript-content</code> after <code>mcp</code> only if the agent should be\nable to search and read archived transcript text.</p>\n</li>\n<li>\n<p>Start the proxy and point each client at it as described in\n<a href=\"https://github.com/hraness/gobstopper/blob/main/docs/proxy.md\">docs/proxy.md</a>.</p>\n</li>\n</ol>\n<p>Hook installation and removal export candidates without changing provider settings.\nThe bundle includes the exact original settings and hashes, so keep it private.\nAutomatic settings replacement is disabled because Gobstopper cannot obtain\ncustody honored by provider/editor writers. Review and apply candidates through\nprovider-owned settings controls and retain provider trust prompts. Callbacks\narchive source-bound evidence; their session identifiers do not prove which\noperation caused a compaction. See <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/assurance/operations.md\">the recovery runbook</a>.</p>\n<p>For automation, <code>gobstopper plan &lt;session&gt; --json</code> returns the existing plan\nobject when a plan is available. A successful inspection without a plan returns\na separate JSON result, for example:</p>\n<pre><code class=\"language-json\">{\n  &quot;status&quot;: &quot;no_plan&quot;,\n  &quot;reason_code&quot;: &quot;below_trigger&quot;,\n  &quot;context_tokens_before&quot;: 100000,\n  &quot;effective_trigger_tokens&quot;: 250000,\n  &quot;target_context_tokens&quot;: 40000,\n  &quot;min_savings_tokens&quot;: 4096,\n  &quot;projected_context_tokens_after&quot;: null,\n  &quot;projected_savings_tokens&quot;: null\n}\n</code></pre>\n<p>The reason identifies the decision actually reached:</p>\n<table>\n<thead>\n<tr><th><code>reason_code</code></th><th align=\"right\">Meaning</th></tr>\n</thead>\n<tbody>\n<tr><td><code>below_trigger</code></td><td align=\"right\">Context is below the effective policy trigger.</td></tr>\n<tr><td><code>strategy_returned_no_plan</code></td><td align=\"right\">The strategy declined; its underlying reason is unknown.</td></tr>\n<tr><td><code>empty_external_edits</code></td><td align=\"right\">The configured command or plugin supplied no edits.</td></tr>\n<tr><td><code>minimum_savings_not_met</code></td><td align=\"right\">A proposal fell short of the minimum projected savings.</td></tr>\n<tr><td><code>external_nonreducing_plan</code></td><td align=\"right\">An external proposal did not reduce estimated context.</td></tr>\n</tbody>\n</table>\n<p>Projections are present only when a rejected proposal supplied them.\nThe target is a policy setting, not a measured\nminimum context size, and projected savings are not billed savings. Invalid\nconfiguration, invalid proposals, and execution failures are command errors.</p>\n<p>Gobstopper's copy paths require retained source and candidate bytes before\npublication. File-copy paths publish a separate candidate after structural\nverification. Native dispatch remains guarded even if a policy proposes it;\nstandalone native <code>apply</code> refuses before creating a fork or snapshot. Legacy\ndirect-write flags remain readable but cannot authorize those writes. A real\nwatch pass can archive a source snapshot before reaching the native guard;\n<code>watch --dry-run</code> does not create that snapshot.\nTelemetry is best effort:\nsuccessful event writes use the <code>gobstopper/compaction-events-v1</code> schema.</p>\n<p><code>eval</code> and <code>bench</code> freeze each session's source before comparing strategies.\n<code>bench</code> selects sessions updated within seven days by default;\n<code>--all</code> removes that age filter but retains discovery and input limits. Its\n24-column CSV includes source/result hashes, <code>execution_state</code>, <code>token_basis</code>,\nretention availability and a closed <code>failure</code> category. Discovered sessions\nthat fail policy resolution or evaluation remain explicit failed rows with\nunavailable measurements. Parse CSV quoting rather than splitting lines or\ncommas: session identifiers can contain those characters. A provider proposal\nis <code>provider_not_executed</code>; a detached transform is not a resumed provider\nsession. Numeric legacy fields must be read with those state and availability\nfields, not counted as measured zeroes or task success.</p>\n<h3 id=\"typed-retention-experiments-opt-in\">Typed-retention experiments (opt-in)</h3>\n<p><code>eval-study</code> replays four arms on isolated in-memory candidates: an unchanged\n<code>no_compaction</code> baseline; plain\nobservation masking; typed masking (constraints, procedures, and open tasks\nstay pinned in their original records and roles, and retrieved text never\nbecomes a higher-authority instruction); and <code>typed_digest</code> (pinned records\nare elided but their spans are carried verbatim on an injected state card,\nwhich loses the original record and role just as a summary does). It does not\nchange <code>auto</code>, call a model, emit live compaction telemetry, or modify the\nprovider session. A requested floor may remain unreachable rather than\ndropping a pinned item.</p>\n<pre><code class=\"language-sh\">gobstopper eval-study /private/source.jsonl --prepare-manifest /private/checks.json\ngobstopper eval-study /private/source.jsonl --manifest /private/checks.json --rounds 10 --trigger 1 --floor 40000 --json\n</code></pre>\n<p>Preparation refuses an existing destination and writes only hashes, byte spans,\nJSON pointers, types, and opaque check IDs, not transcript text. Its labels are\nheuristic candidates that nobody has reviewed: at most 16 complete lines per type,\nwith elidable records considered first and source order breaking ties. Reviewed\nmanifests can instead use <code>label_source = &quot;reviewed&quot;</code>; classification coverage\nis not measured by retention. The JSON schema is <code>gobstopper-retention-v1</code>, with\n<code>source_sha256</code>, <code>label_source</code>, and <code>checks</code> entries containing <code>id</code>, <code>kind</code>,\n<code>record_index</code>, <code>pointer</code>, <code>start_byte</code>, <code>end_byte</code>, and <code>sha256</code> of that exact\nUTF-8 span. Types are <code>constraint</code>, <code>procedure</code>, <code>open_task</code>, <code>fact</code>, <code>preference</code>,\nand <code>episode</code>. Only the first three are pinned. Source identity, live context,\ntext-only pointers, span boundaries, duplicate IDs, and hashes are checked\nbefore any replay. Limits: 64 MiB of source for replay (512 MiB, the vault\nlimit, for score-only manifest prep and <code>--against</code> audits),\n1 MiB of manifest, 256 checks, 4 KiB per span, and 1–10 rounds.</p>\n<p>The report separates text presence, same-origin presence, and preservation at\nthe original source record/pointer. <code>lexical_retained</code> is a paraphrase-sensitive\nmiddle tier: a check counts when ≥75% of its normalized content tokens\n(lowercase alphanumeric, ≥4 chars, stopwords removed) appear together in one\nlive slot. That helps when a provider summary rephrases rather than repeats,\nbut it measures token coverage, not semantic equivalence. <code>by_kind</code> holds <code>[total, source-bound retained, lexical retained]</code>; the elidable subset is reported\nseparately. Dead\nbranches and metadata cannot satisfy a check. Pre-existing source verification\nerrors and newly introduced errors are counted separately. Counts are not\nsemantic or behavioral scores. Estimated context uses adapter item estimates, not stale provider usage records\nor billing. All arms use the same policy, including minimum savings and the\nprotected recent tool-output tail.</p>\n<p><code>--against AFTER</code> switches to a score-only realized audit: the manifest binds\nto the session's before-state and retention is scored against independent\nafter-bytes, with no replay and no mutation. Either spec may be a <code>vault:&lt;sha256&gt;</code>\nsnapshot reference. <code>scripts/retention-audit.py</code> scans the vault for consecutive\nsnapshots whose provider compaction-marker count increased (Claude\n<code>compact_boundary</code>, Codex <code>&quot;type&quot;:&quot;compacted&quot;</code>; hook bracket labels alone can\nmiss the actual write), pairs surgery-labeled snapshots with the next\nsnapshot, and runs the audit over each pair. The result is realized, per-kind\nretention of compactions that already happened, including provider-native\nones.</p>\n<p>Without new work, replay is explicitly <code>static_stress</code>; unchanged passes do not\ncount as applied compactions. For Codex/Claude fixtures, optional <code>growth</code>\nentries (<code>after_round</code>, <code>records</code>) append complete provider records between\nrounds and are verified before use. Checks still refer to the initial source;\nthis is not a test of revised tasks, independent tasks, or agent reasoning.\nProvider-native compaction, semantic summarization, continuation success, cost,\nand retrieval are not measured, and the report does not score them as\nsuccessful or free. The built-in <code>structured</code> strategy is not used as a\nsubstitute for a semantic summarizer.</p>\n<p>A pilot can freeze up to eight selected session exports and register its\nprotocol before outcomes. Choose a new private output directory outside Git:</p>\n<pre><code class=\"language-sh\">python3 scripts/compaction-study.py --binary target/release/gobstopper --output /private/new-pilot --session SESSION_ID\n</code></pre>\n<p>It pins the executable, source exports, annotation manifests, and hashes;\nkeeps content private; uses isolated config/telemetry paths; and checks that the\nfrozen inputs remain unchanged. There are no provider calls. Commands have\noutput/deadline limits and the study has a 900-second overall deadline.</p>\n<p>The separate synthetic provider probe makes at most three\nClaude commands, capped at $0.25 each, using an isolated configuration directory,\nno tools, safe mode, and no MCP servers. It requires explicit opt-in and stops\nif that isolated profile is not authenticated; it never copies credentials.\nIt checks for a persisted native compaction boundary before testing recall.\n<code>--seed-style baseline</code> uses explicit test framing; <code>--seed-style naturalistic</code>\nembeds the identical facts in a plausible work narrative; <code>constraints</code> makes\nthe seed rule-dense; <code>pinned</code> keeps the rules out of the transcript entirely:\nthey ride in <code>--append-system-prompt</code>, the provider's own pinned-context\nchannel (safe mode disables CLAUDE.md discovery), while conversational facts\nstill go through the summarizer. <code>claude_md</code> exercises the production pin\nchannel instead: the same rules land in a workspace <code>CLAUDE.md</code> and the arm\ndrops <code>--safe-mode</code> so project memory loads (the isolated config home and\nscratch workspace remain the boundary). Rule-bearing styles add a <code>rules[]</code>\nrecall scored per-marker as <code>constraint_rules_recalled</code>. Recall is scored twice:\nstrict exact match (<code>recall_checks_passed</code>) and containment\n(<code>recall_checks_lenient</code>), so a semantically preserved superset answer is not\nindistinguishable from a lost fact. After interactive login in that isolated\nprofile, a fresh probe output directory can reuse it with\n<code>--auth-home /private/previous-probe/claude-home</code>:</p>\n<pre><code class=\"language-sh\">python3 scripts/provider-retention-probe.py --claude-bin /absolute/path/to/claude --output /private/new-native-probe --allow-provider-calls\n</code></pre>\n<p>A passing synthetic probe is not a four-arm real-session comparison or evidence\nof billed savings. These commands do not change the strategies the watch\ndaemon uses. Design references: <a href=\"https://arxiv.org/abs/2608.22752\">Knowledge Triage</a>,\n<a href=\"https://arxiv.org/abs/2508.21433\">The Complexity Trap</a>,\n<a href=\"https://arxiv.org/abs/2606.23525\">SelfCompact</a>,\n<a href=\"https://arxiv.org/abs/2510.00615\">ACON</a>, and\n<a href=\"https://arxiv.org/abs/2410.10813\">LongMemEval</a>.</p>\n<p>Config: <code>~/.config/gobstopper/config.toml</code></p>\n<pre><code class=\"language-toml\">[policy]\nstrategy = &quot;auto&quot;\ntrigger_tokens = 250_000\nfloor_tokens = 40_000\nmin_savings_tokens = 4_096   # reject ineffective plans\nadaptive = true              # derive trigger/floor per session; see `gobstopper tune`\n\n[provider.codex]             # per-provider overrides\ntrigger_tokens = 200_000\n\n[sessions.&quot;01a08d7c-…&quot;]      # per-session overrides\nstrategy = &quot;structured&quot;\ntrigger_tokens = 120_000\n\n[presets.deep-work]          # named presets, selectable via --preset\nstrategy = &quot;elide&quot;\ntrigger_tokens = 150_000\n\n[presets.cliff]              # CliffCompaction's rule on a transcript copy\nstrategy = &quot;cliff&quot;\nkeep_recent_turns = 3        # newest assistant steps kept byte-for-byte\nresult_max_bytes = 500       # older tool results above this are dropped\nkeep_recent_tool_outputs = 0 # no extra protected result tail\n\n[presets.custom-script]      # legacy userspace code preset\ncommand = &quot;python3 ~/bin/my_compactor.py&quot;\ntrusted_legacy_command = true\n\n[discovery]\nmax_age_secs = 604800        # rolling window for `watch` and `report`;\n                             # 0 = every session regardless of age\n</code></pre>\n<p>For sessions stored outside the default directories, such as in a sandboxed\nhome, pass <code>--codex-home</code> or <code>--claude-home</code>.</p>\n<h3 id=\"monitoring-an-existing-codex-desktop-session\">Monitoring an existing Codex desktop session</h3>\n<p>Standalone <code>watch</code> cannot compact the context already held by another Codex\nprocess. It reports native delegation as <code>skipped</code>, with zero credited savings;\nthe program running that session has to request the compaction. Without\n<code>--active-only</code>, <code>watch</code> and <code>report</code> consider sessions active within\n<code>[discovery] max_age_secs</code> (7 days by default); <code>watch --max-age</code> and\n<code>report --max-age</code>/<code>--all</code> override it. <code>--active-only</code> limits discovery\nto files updated within the last 180 seconds (a recency heuristic, not proof of\nan owning process), and <code>--once</code> exits after one pass. A dry run writes no forks\nor compaction events. Installed provider-managed lifecycle hooks can archive\nobservations and provide a recovery pointer. They do not establish an applied\nGobstopper operation or a matched before/after pair.</p>\n<p>The optional <a href=\"https://github.com/hraness/gobstopper/blob/main/scripts/monitor.md\">local monitor</a> records numeric observations\nfor an explicit list of sessions and checks a deterministic dry-run watcher.\nIt separates observed context drops, native hook activity, and projected\ncompaction plans; none is automatically counted as Gobstopper-caused usage\nsavings. Current Codex <code>event_msg/token_count</code> accounting and legacy usage\nrecords are both supported, including the advertised model context window.</p>\n<p><code>scored</code> uses the deterministic offline heuristic by default. Experimental\nmodel scoring is opt-in with <code>GOBSTOPPER_SCORER=llm</code>,\n<code>GOBSTOPPER_SCORER=jev</code>, or <code>GOBSTOPPER_SCORER=apple</code>; merely setting an API\nkey never sends data. Remote Jev and LLM scorers receive bounded labels and\nsummaries, including tool arguments, short output tails, and user-prompt\nsnippets. These are transcript-derived text, not redacted metadata; enabling\na remote scorer sends them to its configured endpoint even when additional\ncontent excerpts are disabled. Apple scoring runs on-device. All model\nscorers retain deterministic heuristic scores whenever a model omits an\nanswer or a request fails. Hosted LLM settings are hard-capped\nat 256 candidates, 64 items per batch, 16 batches, and a 100–30,000 ms\ntimeout. The built-in heuristic is the recommended default because the\nrecorded live trials did not show a better plan from the LLM scorer.</p>\n<p>The <code>scored</code> strategy also supports an optional keep-score cutoff. For example,\nadd this named preset to your config:</p>\n<pre><code class=\"language-toml\">[presets.retained]\nstrategy = &quot;scored&quot;\nkeep_score_threshold = 0.5\n</code></pre>\n<p>Inspect it with <code>gobstopper plan &lt;session-id&gt; --preset retained</code>; use the same\nflag with <code>gobstopper apply</code> to create a fork. Candidates\nat or above the cutoff are preserved, even if that prevents reaching the token\ntarget. Missing, invalid, or duplicate candidate scores are also preserved.\nThe value must be finite and between <code>0.0</code> and <code>1.0</code>. The heuristic score is a\nranking signal, not a calibrated probability; <code>0.5</code> is an experimental example,\nnot a tuned recommendation. Model scorers still use heuristic fallback for\npartial or failed responses, so the cutoff does not guarantee provider\nconfidence. Set <code>strategy = &quot;scored&quot;</code> explicitly: <code>auto</code>, <code>compacted</code>, and other\nstrategies ignore the cutoff. Omitting it in a later configuration layer\ninherits an earlier value rather than clearing it. The default configuration\nhas no cutoff.</p>\n<p><code>GOBSTOPPER_SCORER=jev</code> scores with TypeSafe's System One API, which returns\ntyped <code>noul</code> keep-probabilities instead of generated prose. Onboarding vaults the key\nin the OS credential store\n(macOS Keychain, Windows Credential Manager, Linux kernel keyring):</p>\n<pre><code class=\"language-sh\">pbpaste | gobstopper auth jev     # or run it bare to use the clipboard\ngobstopper auth jev --status      # key source + live check; no key fragments\ngobstopper auth jev --delete      # remove the stored key\n</code></pre>\n<p>An API check precedes storage. A definitively rejected key is refused;\nnetwork or API failures allow storage with an explicit unverified result.\nResolution order at scoring time is\n<code>TYPESAFE_API_KEY</code> → <code>GOBSTOPPER_JEV_API_KEY</code> → OS keychain, so CI keeps\nworking from env alone. Linux kernel-keyring entries are session-scoped and\ndo not survive a reboot; use an environment variable for persistent\nnoninteractive Linux automation. On macOS, a self-built unsigned binary may\nshow a one-time keychain access prompt on first read. Remote scorers require\ncurl 8.3+: bearer credentials are imported from a child-only environment\nvariable and expanded inside curl, never placed in process argv; Gobstopper\nalso disables <code>.curlrc</code> for these calls so user defaults cannot enable verbose\nheader logging.\n<code>GOBSTOPPER_JEV_CONTENT_BYTES</code> (default <code>0</code>, maximum <code>1024</code>) opts in to\nattaching additional bounded per-candidate content excerpts to each question.\nThe remote scorer already receives the bounded labels and summaries described\nabove. A post-fix 141k-token A/B run selected the same six records with and\nwithout 400-byte additional excerpts, so those excerpts remain off by default.\nEvery numeric runtime knob is clamped: 1–64 questions per call, 1–128 state\nitems, 100–30,000 ms timeout, 1–16 batches per scoring pass, and 1–4\nconcurrent calls (<code>GOBSTOPPER_JEV_PARALLEL</code>, default <code>2</code>).\n<code>GOBSTOPPER_JEV_MAX_BATCHES</code> defaults to <code>4</code>. Only the newest\n<code>MAX_Q × MAX_BATCHES</code> tailward candidates are sent; an older prefix keeps its\ndeterministic heuristic score. This caps the default at four calls and 256\nremote-scored candidates even for unusually large transcripts. Each logical\nbatch can make two transport attempts if the first has a transient failure.\nIdentical\nquestion texts within a pass are asked once: repeated tool outputs share a\nsingle remote answer instead of being billed per item. Batches run through\na bounded worker pool: execution is parallel, but results are overlaid in\nstable order, a failed or panicked batch retains heuristic scores, and\ncached answers still overlay when a batch's remote half fails. Each pass logs one summary line\nto stderr (candidates, unique/cached/sent questions, calls, failures,\nelapsed). In a historical trial recorded on September 19, 2026, one three-batch\n336k-token Claude session took 148.64s before bounded parallelism and 19.47s\nafterward (about 7.6×). This single-session latency observation does not\nestablish current or provider-wide performance.</p>\n<p><code>eval</code> and <code>bench</code> honor <code>GOBSTOPPER_SCORER</code> for their <code>scored</code> row, so\nan A/B run measures the same Jev or Apple ranking used by <code>plan</code> rather than\nsilently substituting the heuristic. <code>GOBSTOPPER_EVAL_JUDGE=jev</code> adds a\nseparate model-judged retention estimate to <code>eval</code>. Verbatim survivors are\ncredited locally; only up to 64 sampled strings absent from the rewritten live\ncontext become typed <code>noul</code> questions. They are judged against at most 100,000 bytes of bounded\ncompaction evidence (state cards, elision stubs, and short tool records, with\na head+tail fallback). An intact candidate costs no judge request. The judge is\noff by default because missed-fact evaluation sends that bounded evidence to the\nremote API. Invalid or missing answers remain unmeasured; coverage is explicit\nthrough <code>probes_requested</code>, <code>probes_total</code>, <code>complete</code>, <code>recall_available</code> and\n<code>basis</code>. A partial denominator must not be compared as if it covered every\nprobe. Restricting the run to <code>--strategy scored</code> creates at most one logical\njudge request, with one retry allowed on transient transport/5xx failure:</p>\n<pre><code class=\"language-sh\">GOBSTOPPER_SCORER=jev GOBSTOPPER_EVAL_JUDGE=jev \\\n  gobstopper eval &lt;session&gt; --strategy scored\n</code></pre>\n<p>Gobstopper reads the official <code>answers.&lt;id&gt;.noul</code> probability returned by\nSystem One, while retaining bounded compatibility fallbacks for older response\nshapes. Jev starts from the complete deterministic heuristic ranking and\noverlays only valid remote answers; capped candidates, missing answers, and\nfailed or malformed chunks keep their heuristic scores. Semantic eval omits\nunavailable answers instead of crediting unknown facts. These estimates do not\nestablish semantic equivalence, instruction authority, or task success. In one\nhistorical 80k-token A/B run, Jev chose five smaller records where the heuristic chose four larger\nones, reclaimed about 649 more tokens, and both retained all 38 extracted\nprobes. At a more aggressive floor, one probe lost verbatim was not falsely\ncredited by the semantic judge (37/38 on both scores). This is one session,\nnot a general measure of ranking quality.</p>\n<p>Successful Jev answers are cached in-process for five minutes under two\nscopes. The scorer caches each question together with the complete scoring\nstate and model identifier. Unchanged polls reuse judgments, including across\ndifferent question batches. A changed goal or tail requires fresh answers\nwhen that change is represented in the bounded scoring state; the cache cannot\ndetect task changes omitted from that state. The eval judge caches whole exact\nrequests.\nCache keys cover endpoint, credential identity, and question plus scoring\nstate or full request text; values are\nparsed probabilities only (never transcript text), evict oldest-first at\n512 questions and 64 requests, and failures are never cached. The\nquestion layer also persists to\n<code>~/.local/share/gobstopper/jev-cache.json</code> as sha256 key digests mapped to\na probability and timestamp, never text, so a cold <code>plan</code> inside\nthe TTL can reuse an answer when its question and scoring state match.\nThe version 2 disk format rejects malformed, duplicate, future-dated and\nout-of-range entries. Publication uses a new private temporary file, sync and\natomic replacement; concurrent writers can lose reusable entries, causing a\nfresh request, but do not publish a partial image. Older cache formats are\nignored. <code>jev-latest</code> is a provider alias, not an attested weight revision;\nmatching context and TTL do not prove the remote model stayed unchanged.\nTransient transport errors and HTTP 5xx responses are retried once; auth rejections\nare not. The scorer and judge resolve the API key once per process, so\n<code>watch</code> does not re-read the OS credential store every pass. Set\n<code>GOBSTOPPER_JEV_CACHE=0</code> or <code>GOBSTOPPER_JEV_CACHE_TTL_SECS=0</code> to disable\nboth layers;\n<code>GOBSTOPPER_JEV_CACHE_PATH</code> relocates the disk file;\nthe TTL maximum is 3,600 seconds.</p>\n<p><code>GOBSTOPPER_SCORER=apple</code> (macOS 26+, Apple Silicon) scores on-device with\nApple Intelligence Foundation Models via the shared <code>apple-foundation</code>\nbridge with no remote API key. Inference requires an already installed bridge\n(or an explicit <code>GOBSTOPPER_APPLE_BRIDGE</code>); it never builds Swift code on demand.\nUncached requests are serialized, each using one bounded <code>--once</code> process with\nguided JSON output and owned process cleanup. Failure retains heuristic scores.\n<code>GOBSTOPPER_APPLE_TIMEOUT_MS</code>, <code>_MAX_CANDIDATES</code>, <code>_BATCH_SIZE</code>, and\n<code>_MAX_BATCHES</code> tune it, hard-capped at 100–600,000 ms, 256 candidates, 64\nlabels-only items per batch (8 with content), and 16 batches. Since inference\nis local, the scorer also reads a bounded excerpt of each candidate record\n(<code>GOBSTOPPER_APPLE_CONTENT_BYTES</code>, default and maximum 400; <code>0</code> restores\nlabels-only scoring) and shrinks its default batch sizes to fit the ~4k-token\ncontext window. Each batch's guided schema contains one required <code>p_&lt;id&gt;</code> field\nper candidate, so omitted or duplicate array IDs cannot silently distort the\nranking; a malformed batch retains its heuristic scores. Excerpts come from one\nbounded source image whose normalized eligibility and payload fingerprints must\nmatch the plan input; changed or ambiguous sources fall back to the heuristic.</p>\n<p><code>GOBSTOPPER_DIGEST=apple</code> goes further: the injected state card is written\nby the on-device model instead of keyword extraction. Because inference is\nlocal, it may read bounded excerpts of the records being elided without a\nremote request. Each field still\nlands in the same <code>DigestBlock</code> shape via guided output, capped to a small\ntoken overhead, and falls back to the mechanical card on any failure.\n<code>GOBSTOPPER_APPLE_DIGEST_ITEMS</code>, <code>_ITEM_BYTES</code>, and <code>_TOTAL_BYTES</code> tune the\nexcerpt budget, hard-capped at 32 records, 2,048 bytes per record, and 16,000\nbytes total; zero disables the model digest and preserves the mechanical card.</p>\n<p>The same call also writes a one-line stub per excerpted record, such as\n<code>Script completed Wall time 4.4 seconds</code>, stored in the elide edit's\n<code>per_item_stubs</code> map and rendered verbatim in place of the <code>{bytes}</code>/<code>{kind}</code>\ntemplate where the payload was removed. Records the model did not cover\nkeep the generic stub; invalid or oversized stubs are dropped by validation.</p>\n<p>Apple requests are cached in-process by task, instructions, schema, prompt and\nbridge binary identity. Only validated complete scorer batches or validated digest fields/stubs enter\nthe cache. This identifies the exact submitted bounded input, not omitted\nsource context or opaque model weights.\nModel weights and OS inference internals remain opaque; a binary hash does not\nattest their identity. An identical admitted request can reuse its recorded\nresponse without another generation.\nThe cache is bounded at 64 entries with oldest-first eviction;\n<code>GOBSTOPPER_APPLE_CACHE=0</code> disables both reads and writes. Scorer diagnostics\nreport cached batches separately from real model calls. The savings gate\nalso prices the residual stub text left behind by elision so\n<code>context_tokens_after</code> doesn't overstate reclaim.</p>\n<p>With <code>adaptive = true</code>, the effective trigger/floor are re-derived per\nsession at each decision point: the trigger is capped at a quarter of\nthe provider-advertised context window, backed off (bounded 2x) when\nrecent compactions reclaimed too little to be worth a cycle, and\ntightened when most of the window is reclaimable tool output. The\nadjustment is deterministic and its reasons appear in plan output and\ntelemetry. <code>gobstopper tune &lt;session&gt;</code> previews it.</p>\n<h2 id=\"how-gobstopper-compares-with-cliffcompaction\">How Gobstopper compares with CliffCompaction</h2>\n<p><a href=\"https://github.com/nguyenvuthientrang/cliffcompaction\">CliffCompaction</a> is\nan open-source (MIT) API proxy for coding agents by Trang Nguyen, Eulrang Cho,\nBingqing Chen, and Tim Dettmers, described in\n<a href=\"https://arxiv.org/abs/2609.26779\">arXiv:2609.26779</a> (September 2026). You\npoint an agent's base URL at it. When a request exceeds a token threshold, the\nproxy sends the system prompt and task verbatim, then one mechanical summary of\nthe older turns, then the last three turns verbatim. The summary keeps tool\nresults of at most 500 characters, drops longer ones because the files behind\nthem are still readable, reduces tool calls to one-line signatures, and keeps\nassistant text. Each later compaction is rebuilt from the original history the\nagent resends, and the previous summary is discarded: the authors call this\nnever compacting a compaction. Their paper reports up to 50% lower cost at a\nbounded context with maintained or improved Terminal-Bench 2.0 results for the\nKimi and GLM models they tested; those are the authors' benchmark figures, not\nmeasurements of Gobstopper.</p>\n<p>Gobstopper runs the same summary rule in its own proxy, keeps a larger recent\ntail by default, and also works on saved session files:</p>\n<table>\n<thead>\n<tr><th></th><th align=\"right\">CliffCompaction</th><th align=\"right\">Gobstopper</th></tr>\n</thead>\n<tbody>\n<tr><td>Where it runs</td><td align=\"right\">A local HTTP proxy between the agent and the Anthropic or OpenAI API</td><td align=\"right\">A local HTTP proxy between the agent and its model provider, plus a CLI over the session files Claude Code and Codex write</td></tr>\n<tr><td>Clients</td><td align=\"right\">Any client of the Anthropic Messages, OpenAI Chat Completions, or OpenAI Responses API</td><td align=\"right\">Any client of the same three dialects that accepts a custom provider address: Claude Code, Codex, opencode, Crush, Aider, Goose, and more</td></tr>\n<tr><td>What it changes</td><td align=\"right\">Each outgoing request, transparently, while the session runs</td><td align=\"right\">The proxy rewrites outgoing requests over the threshold; file commands publish a separate compacted copy and leave the source unchanged</td></tr>\n<tr><td>How it shrinks</td><td align=\"right\">Drops tool results over 500 characters, signatures for tool calls, last three turns verbatim; never paraphrases</td><td align=\"right\">The proxy applies the same summary rule and keeps at least the last three turns verbatim, plus older whole turns that fit in 40% of the room under the threshold; file strategies drop or stub stale tool results, and <code>structured</code> and <code>compacted</code> add a metadata state card; no built-in strategy paraphrases unless <code>GOBSTOPPER_DIGEST=apple</code> has an on-device model write the card</td></tr>\n<tr><td>Recompaction</td><td align=\"right\">Rebuilt from the original history; the prior summary is discarded</td><td align=\"right\">The proxy rebuilds from the original history; <code>cliff</code> on a copy drops the same records as one pass over the source when both passes produce a plan; strategies that inject a state card carry it forward into the next copy</td></tr>\n<tr><td>What holds the originals</td><td align=\"right\">The agent's own history and the files on disk; the proxy keeps only an in-memory cache of compacted prefixes</td><td align=\"right\">For proxied requests, the agent's own transcript and an in-memory cache; for copies, a content-addressed vault with <code>search-snapshot</code> and <code>read-snapshot</code></td></tr>\n<tr><td>Evidence published</td><td align=\"right\">Terminal-Bench 2.0 and 2.1 (including a run through Claude Code), SWE-bench Verified, and KernelBench results in the paper, on Kimi, GLM, and GPT-5-mini models</td><td align=\"right\">Offline replays of 729 archived sessions, replays of nine recorded sessions through the proxy, one dated afternoon of live proxy counters, literal retention probes, and dated single-session trials; no task-success or billing claims</td></tr>\n<tr><td>Model needed</td><td align=\"right\">None; the summary is mechanical</td><td align=\"right\">None for the proxy or the built-in strategies; optional model scorers</td></tr>\n</tbody>\n</table>\n<p><code>gobstopper proxy</code> is a Rust port of CliffCompaction's request engine: the\nsame summary format and header, prefix reuse between compactions, harsher\nsettings when one pass leaves a request over the threshold, and a retry when\nthe provider rejects a request for length. It departs from the reference in\nfive ways. It keeps older whole turns verbatim beyond the last three while\nthey fit its tail budget; <code>--keep-tail-percent 0</code> keeps the reference tail,\nexcept for the next rule. It counts a run of consecutive assistant messages\nas one turn in every dialect, where the reference does so only for\nResponses. Claude Code can record one step as two assistant messages, tool\ncalls and then text; the Anthropic API merges them, so a split between them\nwould separate the calls from their results. Anthropic requests that declare\na 1M-token window use a separate threshold, <code>--threshold-1m</code>. When the\nprovider rejects a rewritten request for another reason, it resends the\noriginal. When the verbatim head alone approaches the threshold, it raises\nthe threshold instead of compacting every request. The port's MIT notice is\nin <a href=\"https://github.com/hraness/gobstopper/blob/main/THIRD_PARTY_NOTICES.md\">THIRD_PARTY_NOTICES.md</a>.</p>\n<p>The <code>cliff</code> strategy applies the drop rule to a transcript copy instead:\nthe head and the newest <code>keep_recent_turns</code> assistant steps stay\nbyte-for-byte, older tool results over <code>result_max_bytes</code> are dropped unless they are among\nthe newest <code>keep_recent_tool_outputs</code> (default 8), smaller ones stay, and nothing is summarized or added. A step starts where the\nassistant side resumes after a user prompt or a tool result and includes the\ntool results that answer it. Tool-call signatures and reasoning caps are not\npart of the file transform, because Gobstopper's copy transforms only replace\ntool-result payloads. Codex <code>compacted</code> records count as one result. When both\npasses run at the same cut and both produce a plan, the records dropped from\nthe source and then from the copy are, together, the records a single\ncompaction from the source would drop; one unit test checks this on a\nsynthetic transcript. A copy below the trigger or the minimum savings is not\ncompacted again, so under the default policy the two paths can differ. The\ndropped bytes stay in the vault, not in the copy.</p>\n<pre><code class=\"language-sh\">gobstopper plan &lt;session&gt; --strategy cliff\ngobstopper eval &lt;session&gt;               # cliff appears beside the other strategies\n</code></pre>\n<p><code>auto</code> does not select <code>cliff</code>; choose it explicitly or through a preset. Run\none proxy per client: chaining CliffCompaction and <code>gobstopper proxy</code> would\ncompact each other's output, and the two have not been tested together. The\ncomparison page at\n<a href=\"https://gobstopper.sh/compare/cliffcompaction\">gobstopper.sh/compare/cliffcompaction</a>\ncarries the same table.</p>\n<h2 id=\"integrating-with-a-session-runtime\">Integrating with a session runtime</h2>\n<p>A program that runs agent sessions can ask Gobstopper what to do without\ngiving it transcript access. <code>policy-check</code> takes the numbers the runtime\nalready tracks, such as current context tokens and whether the session is\nactive, and returns an action:</p>\n<pre><code class=\"language-sh\">gobstopper policy-check --provider codex --context-tokens 300000 \\\n    --session-active --json\n# {&quot;action&quot;:&quot;provider_compact&quot;,&quot;control&quot;:&quot;thread/compact/start&quot;, ...}\n</code></pre>\n<p><code>policy-check</code> returns a decision; it does not call the provider. The runtime\nmust establish that it controls the selected session and test the provider's\noperation before executing it on its own connection. File preparation publishes\nseparate copies for an explicit resume; provider acceptance is a separate check.</p>\n<p>This numeric interface was designed for the session runtime that preceded\nxcb, retired on 2026-09-19. xcb embeds <code>gobstopper-core</code> as a library; see the\n<a href=\"https://github.com/hraness/gobstopper/blob/main/docs/plugin-protocol.md\">plugin protocol</a>. The\n<a href=\"https://github.com/hraness/gobstopper/blob/main/docs/integration-contract.md\">historical integration contract</a> records the\noriginal interface and the obligations of a runtime that uses it.</p>\n<h2 id=\"provider-levers-observed-in-earlier-versions\">Provider levers observed in earlier versions</h2>\n<p>These are protocol notes from earlier provider builds, not an activation grant\nfor the installed version. The <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/assurance/qualification.json\">qualification matrix</a>\nrecords current status; there are no qualified live native cells.</p>\n<table>\n<thead>\n<tr><th>lever</th><th align=\"right\">codex</th><th align=\"right\">claude code</th></tr>\n</thead>\n<tbody>\n<tr><td>auto-compact threshold</td><td align=\"right\"><code>model_auto_compact_token_limit</code> (config; ≤90% of window)</td><td align=\"right\"><code>--autocompact &lt;100k–1M&gt;</code> argv</td></tr>\n<tr><td>on-demand trigger</td><td align=\"right\"><code>thread/compact/start</code> (app-server v2)</td><td align=\"right\"><code>/compact [instructions]</code></td></tr>\n<tr><td>compaction prompt</td><td align=\"right\"><code>compact_prompt</code> config</td><td align=\"right\"><code>/compact</code> instructions</td></tr>\n<tr><td>tool output cap</td><td align=\"right\"><code>tool_output_token_limit</code></td><td align=\"right\">none</td></tr>\n<tr><td>live usage stream</td><td align=\"right\"><code>thread/tokenUsage/updated</code> notification</td><td align=\"right\"><code>message.usage</code> per turn</td></tr>\n<tr><td>transcript store</td><td align=\"right\"><code>~/.codex/sessions/**/rollout-*.jsonl</code></td><td align=\"right\"><code>~/.claude/projects/*/*.jsonl</code></td></tr>\n</tbody>\n</table>\n<p>Codex persists compaction as a <code>compacted</code> rollout record carrying\n<code>replacement_history</code>, the context Codex loads in place of the earlier\nhistory when it resumes.\ngobstopper's parser and verifier understand that shape, including tool pairs\ninside <code>replacement_history</code>. Synthetic records are experimental, pair-aware,\nchecked before copy publication, and available only behind <code>--experimental-compacted</code>; ordinary\n<code>apply</code> uses the portable forked digest representation.</p>\n<p>The CLI contains native adapters and a durable operation journal, but release\nbuilds refuse dispatch with <code>native_unqualified</code>, including prior\n<code>auto_compact_closed</code> settings. Debug protocol fixtures require exact synthetic\nexecutable bytes and isolated temporary homes. They do not qualify a live provider.\nIf an earlier attempt is dispatched or unknown, cooldown expiry, changed source\nbytes, and watch restarts cannot automatically replay it. Inspect\n<code>gobstopper native-operations</code>; reconciliation accepts only persisted matching\nCodex terminal identity, never a caller-supplied success flag. See the\n<a href=\"https://github.com/hraness/gobstopper/blob/main/docs/assurance/operations.md\">recovery runbook</a>.</p>\n<p>The compatibility settings <code>auto_apply_inplace</code>, <code>auto_apply_store</code>, and\n<code>auto_compact_closed</code> cannot enable these disabled operations. See\n<a href=\"https://github.com/hraness/gobstopper/blob/main/config.example.toml\">config.example.toml</a> for their current meanings.</p>\n<h2 id=\"layout\">Layout</h2>\n<ul>\n<li><code>crates/gobstopper-core</code>: the normalized transcript model, the <code>Edit</code> IR,\nthe <code>Strategy</code> trait, all built-in strategies, and the telemetry schema.\nIts only file I/O is the telemetry event log.</li>\n<li><code>crates/gobstopper-adapters</code>: session discovery, Codex and Claude Code JSONL\nparsing and copy preparation, no-clobber publication, verification, plugin\nhosting, and the snapshot vault.</li>\n<li><code>crates/gobstopper-cli</code>: the <code>gobstopper</code> binary (run <code>gobstopper --help</code>\nfor every subcommand), layered configuration, hooks, and the read-only MCP\nserver.</li>\n</ul>\n<h2 id=\"benchmark-results\">Benchmark results</h2>\n<p>The <a href=\"https://gobstopper.sh/benchmarks#retrospective-2026-09-19\">September 19, 2026 retrospective</a>\nevaluated 729 frozen sessions on one Mac. Portable <code>compacted</code> projected a\n36.4% median reduction across 73 high-context archived Codex roots, with\n76.9% sampled-string retention. Across all 729 sessions, 637 produced no\nplan and the median reduction was 0%. These are offline projections, not\nbilling savings or task-accuracy measurements. The page includes all cohorts,\nlimitations, and downloadable aggregate results and methodology.</p>\n<p>A separate <a href=\"https://gobstopper.sh/benchmarks#retention-policy-2026-09-19\">paired scored-policy replay</a>\nreused the 114 archived roots with the corrected probe limit. This development\ncomparison is not held-out validation; the original baseline was already known.\nOn the 73\nhigh-context tasks, a <code>0.35</code> cutoff increased sampled-string retention from\n77.05% to 80.78% while median projected reduction fell from 36.44% to 34.96%.\nTwo tasks produced no plan, and their retention is derived from leaving the\nsource unchanged. All three registered cutoffs and both disabled baselines are\nreported, and the default has no cutoff. The result is a small trade of\nsize for retention; it does not measure task quality or recommend a cutoff.</p>\n<h2 id=\"historical-live-trials-recorded-september-17-2026\">Historical live trials, recorded September 17, 2026</h2>\n<p>The following single-session experiments were recorded in the repository on\nSeptember 17 using earlier builds and workflows. They are separate from the\n729-session retrospective and do not establish current provider-wide savings\nor general task quality. They do not qualify this artifact's activation matrix.\nOne 333k-token Claude session was asked the same\nresume question under four conditions, measuring provider tokens on that turn:</p>\n<table>\n<thead>\n<tr><th>condition</th><th align=\"right\">input tokens on resume</th><th align=\"right\">output tokens</th><th align=\"right\">recalled the standing task?</th></tr>\n</thead>\n<tbody>\n<tr><td>none (original)</td><td align=\"right\">312,722</td><td align=\"right\">1,405</td><td align=\"right\">yes: reported npm unification and stalled renames</td></tr>\n<tr><td><code>gobstopper elide</code></td><td align=\"right\">219,167</td><td align=\"right\">1,052</td><td align=\"right\">yes: same standing task, stalled renames</td></tr>\n<tr><td><code>gobstopper compacted</code></td><td align=\"right\">220,447</td><td align=\"right\">621</td><td align=\"right\">yes: same standing task from the state-card digest</td></tr>\n<tr><td><code>claude --autocompact 100</code></td><td align=\"right\">56,300</td><td align=\"right\">416</td><td align=\"right\">no: incorrectly claimed the renames were already done and published</td></tr>\n</tbody>\n</table>\n<p><code>gobstopper elide</code> and <code>compacted</code> both cut the resume context by about\n30% while keeping the answer accurate. Claude's native <code>--autocompact 100</code>\ncut the resume context by ~82% but produced a confident, inaccurate\nsummary of the session.</p>\n<p>The same question was then asked on a 101k-token Codex session:</p>\n<table>\n<thead>\n<tr><th>condition</th><th align=\"right\">input tokens on resume</th><th align=\"right\">output tokens</th><th align=\"right\">recalled the standing task?</th></tr>\n</thead>\n<tbody>\n<tr><td>none (original)</td><td align=\"right\">101,275</td><td align=\"right\">244</td><td align=\"right\">yes: Oh's memory benchmark and the 0.60 expansion gate</td></tr>\n<tr><td><code>gobstopper elide</code></td><td align=\"right\">57,980</td><td align=\"right\">83</td><td align=\"right\">yes: same 0.545 score and 0.60 gate</td></tr>\n<tr><td><code>gobstopper compacted</code></td><td align=\"right\">34,503</td><td align=\"right\">159</td><td align=\"right\">yes: same BEAM experiment and expansion gate</td></tr>\n</tbody>\n</table>\n<p>On Codex, <code>compacted</code> cut resume input tokens by 66% and <code>elide</code> cut\nthem by 43%, both with accurate answers to that question. Provider-native\nCodex compaction was not included in this historical trial.</p>\n<p>The same-session <code>cache_aware</code> A/B (339k-token Claude session, floor 310k,\nreal provider cache counters):</p>\n<table>\n<thead>\n<tr><th>condition</th><th align=\"right\"><code>cache_read</code></th><th align=\"right\"><code>cache_creation</code></th><th align=\"right\">file-level prefix preserved</th><th align=\"right\">cost</th><th>accurate?</th></tr>\n</thead>\n<tbody>\n<tr><td>none (original)</td><td align=\"right\">10,010</td><td align=\"right\">325,647</td><td align=\"right\">n/a</td><td align=\"right\">$6.53</td><td>yes</td></tr>\n<tr><td><code>gobstopper cache_aware</code></td><td align=\"right\">13,536</td><td align=\"right\">258,517</td><td align=\"right\">107,884 tokens</td><td align=\"right\">$5.19</td><td>yes</td></tr>\n<tr><td><code>gobstopper compacted</code></td><td align=\"right\">13,536</td><td align=\"right\">257,505</td><td align=\"right\">6,639 tokens</td><td align=\"right\">$5.18</td><td>yes</td></tr>\n</tbody>\n</table>\n<p>In this trial, <code>cache_aware</code> and <code>compacted</code> had almost the same API cost and\nboth recorded 13,536 cache-read tokens, versus 10,010 in the baseline.\n<code>cache_aware</code> preserved 16x more identical transcript prefix. That makes the\nrewrite easier to audit; this trial did not establish extra provider cache savings\nfrom the preserved prefix. Current <code>compacted</code> uses a portable forked digest;\nsynthetic Codex-native records require <code>--experimental-compacted</code>.</p>\n<p>Snapshots and <code>gobstopper diff</code> make compaction inspectable and provide a\nrecovery path. They do not guarantee that omitted facts are unimportant or\nthat a continuation will retrieve them automatically.</p>\n<p>In the same period, a Gobstopper-written Codex <code>compacted</code> record with a\ncorrect window chain was accepted by <code>codex exec resume</code>, and the model\ncompleted a real API turn that recalled the elided commands. In a separate\nClaude Code trial on a 333k-token test copy, Gobstopper elided 43 stale tool\nrecords and injected a state card, and <code>claude --resume</code> succeeded and\nrecalled the standing task. These trials used earlier builds and do not establish current provider resume\ncompatibility. Current <code>apply</code> writes Claude Code compactions to a separate fork.</p>\n<h2 id=\"status\">Status</h2>\n<p>The <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/correctness-audit.md\">correctness audit</a> records established behavior,\nknown defects and evidence limits; the <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/correctness-plan.md\">assurance plan</a>\ntracks the remaining work. No whole-system correctness proof is claimed.\nFile-copy publication uses source hashes, no-clobber creation, retained source\nand candidate bytes, structural checks and durable operation receipts. Shared\nvault readers coordinate with pruning, which fails closed on corrupt recovery\nroots. Operation pins have no automatic retirement policy. Transcript processing\ndefaults to 512 MiB and 100,000 records. Process-death fixtures and\n<a href=\"https://github.com/hraness/gobstopper/blob/main/verify/vault/README.md\">TLA+ vault models</a>, the\n<a href=\"https://github.com/hraness/gobstopper/blob/main/verify/watch/README.md\">native dispatch model</a>,\n<a href=\"https://github.com/hraness/gobstopper/blob/main/verify/core/README.md\">Kani proofs of selected production Rust kernels</a>, and\n<a href=\"https://github.com/hraness/gobstopper/blob/main/verify/transcript/README.md\">Lean transcript algebra</a> cover their declared\ninvariants and bounds; the TLA+ models check safety only, without fairness, so they make no eventual-completion claim. The <a href=\"https://github.com/hraness/gobstopper/blob/main/verify/stress/README.md\">bounded synthetic stress gate</a>\nexercises named fault, restart and process fixtures. The\n<a href=\"https://github.com/hraness/gobstopper/blob/main/verify/README.md\">verification guide</a> describes reproducible tool inputs and CI\ngates. These checks do not prove that the Rust code implements the TLA+ models\nor the Lean algebra (that link is reviewed, or tested on finite fixtures), arbitrary filesystem power-loss\nbehavior, proprietary provider acceptance, or preservation of every task fact.</p>\n<p>Direct provider controls still belong to the live session owner. Synthetic\nCodex <code>compacted</code> records, external model scoring, and semantic editor plugins\nremain explicitly experimental or trusted extension paths. Released\nnative dispatch remains guarded for both providers. In-place and\narbitrary-path rewrite APIs refuse mutation. Deterministic MCP inspection rejects\nexecutable strategies; explicitly invoked extensions remain trusted code rather\nthan an OS sandbox. The <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/assurance/qualification.json\">activation matrix</a>\nand <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/assurance/operations.md\">recovery runbook</a> define the supported modes.\nSee <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/design.md\">docs/design.md</a>, <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/roadmap.md\">docs/roadmap.md</a>, and\n<a href=\"https://github.com/hraness/gobstopper/blob/main/docs/plugin-protocol.md\">docs/plugin-protocol.md</a> for details.</p>\n<h2 id=\"license\">License</h2>\n<p>MIT OR Apache-2.0</p>\n";
