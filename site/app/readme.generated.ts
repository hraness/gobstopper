// Generated from ../README.md by scripts/sync-readme.ts. Do not edit.
export const readmeTitle = "gobstopper";
export const readmeLead = "Automatic context compaction for coding-agent sessions — built-in Codex and Claude Code adapters, native policy integration for Devin, and a bounded provider/strategy plugin protocol.";
export const readmeHtml = "<h1 id=\"gobstopper\">gobstopper</h1>\n<p>Automatic context compaction for coding-agent sessions — built-in Codex\nand Claude Code adapters, native policy integration for Devin, and a bounded\nprovider/strategy plugin protocol.</p>\n<p>gobstopper is a cross-provider context compactor with exact, content-addressed\nrecovery snapshots, resumable Claude Code and Codex transcript forks, and a\nmeasurement harness for projected savings, preserved prefix, structural\nvalidity, and probe recall.</p>\n<p>Run it, and it watches supported agent sessions. When a session's context\ncrosses a configured threshold, gobstopper prepares a separate compacted fork\nusing a strategy selected per session, provider, or preset. The source is\nsnapshotted and never overwritten by standalone <code>apply</code> or <code>watch</code>.</p>\n<h2 id=\"why\">Why</h2>\n<p>Providers compact late. Codex fires auto-compaction at ~90% of the model's\ncontext window; Claude Code similar. On a 1M-token window that means every\nturn near the end of a long session costs ~900k input tokens — and on\nsubscription plans, those tokens come out of your weekly allowance.</p>\n<p>Context is a sawtooth problem. If you compact at trigger <code>T</code> and the\nsummary floor is <code>F</code>, steady-state <em>context occupancy</em> per turn is roughly\n<code>(T+F)/2</code>:</p>\n<table>\n<thead>\n<tr><th>policy</th><th align=\"right\">trigger</th><th align=\"right\">floor</th><th align=\"right\">avg context/turn</th><th align=\"right\">relative occupancy</th></tr>\n</thead>\n<tbody>\n<tr><td>provider default (1M window)</td><td align=\"right\">~900k</td><td align=\"right\">~60k</td><td align=\"right\">~480k</td><td align=\"right\">1.0x</td></tr>\n<tr><td>gobstopper default</td><td align=\"right\">250k</td><td align=\"right\">40k</td><td align=\"right\">~145k</td><td align=\"right\">~3.3x lower occupancy</td></tr>\n<tr><td>gobstopper aggressive</td><td align=\"right\">150k</td><td align=\"right\">20k</td><td align=\"right\">~85k</td><td align=\"right\">~5.6x lower occupancy</td></tr>\n</tbody>\n</table>\n<p>This is an <strong>occupancy model</strong>, not a measured subscription savings claim.\nActual token cost depends on cache hit rates, whether summary turns are\nbilled, re-fetches caused by lost detail, and how often compaction itself\nruns. Anthropic documents that clearing tool results can\n<a href=\"https://platform.claude.com/docs/en/build-with-claude/context-editing\">invalidate the prompt cache</a>.\ngobstopper reports measured file-byte changes and observed provider usage\nwhere available; it does not project dollar or quota savings into its\npublic claims.</p>\n<h2 id=\"infinite-memory\">Infinite memory</h2>\n<p>Every standalone compaction writes the exact source bytes into a\ncontent-addressed vault (<code>~/.local/share/gobstopper/vault/</code>) before publishing\na fork. Snapshots use deduplicated 1 MiB chunks, so appended versions reuse\nunchanged prefix storage without creating one filesystem object per JSONL\nrecord.</p>\n<p><code>gobstopper recall --query &lt;q&gt;</code> turns that vault into agent-addressable\nmemory: it searches every archived state-card digest, ranks results by\nquery relevance, and returns the high-level state of the matching turns.\nThe agent does not need to remember session IDs — it can ask for the last\ntime it worked on a file, a goal, or a decision and get a ranked summary\nwith a snapshot SHA it can <code>show</code> or <code>diff</code>.</p>\n<p><code>gobstopper mcp</code> exposes a read-only Model Context Protocol server on stdio —\ntools <code>policy_check</code>, <code>list_sessions</code>, <code>recall</code>, <code>history</code>, <code>show</code>, <code>diff</code>,\n<code>plan</code>, and <code>verify</code>. Register it once and an agent can inspect policy and\narchived state without gaining a transcript mutation tool:</p>\n<pre><code class=\"language-sh\">claude mcp add gobstopper -- gobstopper mcp\n# ~/.codex/config.toml: [mcp_servers.gobstopper] command = &quot;gobstopper&quot;, args = [&quot;mcp&quot;]\ndevin mcp add -s user gobstopper -- gobstopper mcp\n</code></pre>\n<p>For Devin, <code>policy_check</code> accepts <code>provider = &quot;devin&quot;</code> and returns <code>/compact</code>\nwhen the configured threshold is crossed. Devin remains the sole owner of its\nsession store; gobstopper does not edit Devin history. <code>devin --export out.json</code>\ncan be inspected through a read-only provider plugin when offline analysis is\nneeded.</p>\n<p>Compaction itself isn't free — each cycle costs one large input call and\nrisks losing detail — so strategy matters. That is the actual product\nhere: not &quot;compact earlier&quot; but &quot;compact with the right strategy at the\nright boundary.&quot;</p>\n<h2 id=\"strategies\">Strategies</h2>\n<table>\n<thead>\n<tr><th>id</th><th align=\"right\">kind</th><th align=\"right\">what it does</th></tr>\n</thead>\n<tbody>\n<tr><td><code>auto</code> (default)</td><td align=\"right\">dynamic</td><td align=\"right\">live sessions delegate to provider controls (<code>cache_edits</code> for eligible Claude sessions); idle sessions choose the best validated file strategy by savings and preserved-prefix score</td></tr>\n<tr><td><code>sawtooth</code></td><td align=\"right\">provider</td><td align=\"right\">requests provider-native compaction (<code>thread/compact/start</code> on Codex; <code>/compact</code> guidance on Claude)</td></tr>\n<tr><td><code>cache_edits</code></td><td align=\"right\">provider</td><td align=\"right\">emits bounded Claude <code>tool_use_id</code> values for API-layer context editing; never rewrites a transcript</td></tr>\n<tr><td><code>elide</code></td><td align=\"right\">transcript</td><td align=\"right\">stubs stale tool outputs oldest-first until the floor</td></tr>\n<tr><td><code>cache_aware</code></td><td align=\"right\">transcript</td><td align=\"right\">elides a tailward stale-output window and injects a bounded state card while preserving the longest practical prefix</td></tr>\n<tr><td><code>compacted</code></td><td align=\"right\">transcript</td><td align=\"right\">elides stale outputs and injects the state card; synthetic Codex <code>compacted</code> records require <code>--experimental-compacted</code></td></tr>\n<tr><td><code>scored</code></td><td align=\"right\">transcript</td><td align=\"right\">ranks candidates with deterministic recency, error, reference, TF-IDF, duplicate, and tool-type signals before elision</td></tr>\n<tr><td><code>dedupe</code></td><td align=\"right\">transcript</td><td align=\"right\">removes older exact duplicate tool payloads using payload SHA-256, not summaries</td></tr>\n<tr><td><code>micro</code></td><td align=\"right\">transcript</td><td align=\"right\">keeps the newest configured outputs per stable tool label and stubs older ones</td></tr>\n<tr><td><code>middle</code></td><td align=\"right\">transcript</td><td align=\"right\">protects both ends of the transcript and elides eligible middle outputs</td></tr>\n<tr><td><code>structured</code></td><td align=\"right\">transcript</td><td align=\"right\">emits a bounded metadata-derived state card; it is not semantic summarization</td></tr>\n<tr><td><code>agentic</code></td><td align=\"right\">extension</td><td align=\"right\">accepts bounded edit proposals from an explicitly trusted command or versioned plugin; host validation remains authoritative</td></tr>\n</tbody>\n</table>\n<p>Custom strategies are userspace code: a preset can name a <code>command</code> that\nreceives the normalized transcript as JSON on stdin and returns an edit\nplan on stdout, or install a versioned <code>gobstopper-plugin.json</code> bundle\n(see <code>gobstopper plugin check</code>). Host-side validation bounds every\nproposal: no edit can grow the transcript, leave protected recent output,\nbypass linkage checks, or exceed configured digest size.</p>\n<h2 id=\"install--use\">Install &amp; use</h2>\n<pre><code class=\"language-sh\">cargo install --git https://github.com/hraness/gobstopper gobstopper\n# or from a checkout: cargo build --release\n\ngobstopper detect                  # sessions, context sizes, lifetime burn\ngobstopper plan &lt;session&gt;          # what would happen, under which strategy\ngobstopper plan &lt;session&gt; --trigger 100000 --floor 30000    # tune the trade-off\ngobstopper eval &lt;session&gt;          # every strategy side-by-side on temp copies\ngobstopper apply &lt;session&gt;         # vault snapshot + produce validated fork (idle sessions)\ngobstopper verify &lt;session&gt;        # resume-validity check (exit 1 on errors)\ngobstopper fork &lt;session&gt;          # clone under a fresh session id + resume cmd\ngobstopper undo &lt;session&gt;          # restore a pre-compaction snapshot into a new fork\ngobstopper vault                   # list snapshots in the undo vault\ngobstopper install-hooks           # Claude + Codex compaction lifecycle hooks\ngobstopper watch --dry-run         # the daemon path: poll, threshold, prepare copy\ngobstopper explain                 # the occupancy math above\ngobstopper recall --query &lt;q&gt;      # search state-card digests across all archived sessions\ngobstopper history &lt;session&gt;       # every archived state of one session\ngobstopper diff &lt;sha-a&gt; &lt;sha-b&gt;    # structural comparison of two vault snapshots\ngobstopper bench                   # benchmark every strategy across discovered sessions\ngobstopper tune &lt;session&gt;          # preview the adaptive trigger/floor for a session\ngobstopper mcp                     # read-only MCP server: the vault as agent tools\n</code></pre>\n<p>Every <code>apply</code>/<code>watch</code> compaction snapshots the source transcript into a\ncontent-addressed vault (<code>~/.local/share/gobstopper/vault/</code>) and publishes\nthe result as a separate, verified file. The original transcript is never\noverwritten by a standalone compaction run; live session surgery must be\ndispatched by the session owner. Each compaction appends a numeric record\nto <code>events.jsonl</code> in the <code>gobstopper/compaction-events-v1</code> schema.</p>\n<p>Config: <code>~/.config/gobstopper/config.toml</code></p>\n<pre><code class=\"language-toml\">[policy]\nstrategy = &quot;auto&quot;\ntrigger_tokens = 250_000\nfloor_tokens = 40_000\nmin_savings_tokens = 4_096   # reject ineffective plans\nadaptive = true              # derive trigger/floor per session — see `gobstopper tune`\n\n[provider.codex]             # per-provider overrides\ntrigger_tokens = 200_000\n\n[provider.devin]             # numeric policy only; action is native /compact\ntrigger_tokens = 200_000\n\n[sessions.&quot;01a08d7c-…&quot;]      # per-session overrides\nstrategy = &quot;structured&quot;\ntrigger_tokens = 120_000\n\n[presets.deep-work]          # named presets, selectable via --preset\nstrategy = &quot;elide&quot;\ntrigger_tokens = 150_000\n\n[presets.custom-script]      # legacy userspace code preset\ncommand = &quot;python3 ~/bin/my_compactor.py&quot;\ntrusted_legacy_command = true\n</code></pre>\n<p>Managed sessions (oompa profiles, sandboxed homes) use different roots:\npoint gobstopper at them with <code>--codex-home</code> / <code>--claude-home</code>.</p>\n<p><code>scored</code> uses the deterministic offline heuristic by default. Experimental\nexternal scoring is opt-in with <code>GOBSTOPPER_SCORER=llm</code>,\n<code>GOBSTOPPER_SCORER=jev</code>, or <code>GOBSTOPPER_SCORER=apple</code>; merely setting an API\nkey never sends data. External scorers receive bounded labels and summaries,\nnot full tool payloads, and fall back to the heuristic on failure. The\nbuilt-in heuristic is the recommended published path because current live\ntrials did not show a better plan from the LLM scorer.</p>\n<p><code>GOBSTOPPER_SCORER=jev</code> scores with TypeSafe's System One API — typed\n<code>noul</code> keep-probabilities, ~100ms per batch of 64 questions, no prose\ngeneration. Onboarding vaults the key in the OS credential store\n(macOS Keychain, Windows Credential Manager, Linux Secret Service):</p>\n<pre><code class=\"language-sh\">pbpaste | gobstopper auth jev     # or run it bare to use the clipboard\ngobstopper auth jev --status      # key source + masked value + live check\ngobstopper auth jev --delete      # remove the stored key\n</code></pre>\n<p>The key is verified against the API before it is stored; a rejected key\nnever reaches the keychain. Resolution order at scoring time is\n<code>TYPESAFE_API_KEY</code> → <code>GOBSTOPPER_JEV_API_KEY</code> → OS keychain, so CI keeps\nworking from env alone. <code>GOBSTOPPER_JEV_CONTENT_BYTES</code> (default <code>0</code>)\nopts in to attaching bounded per-candidate content excerpts to each\nquestion — Jev is a remote API, so content only leaves the device when\nexplicitly enabled.</p>\n<p><code>GOBSTOPPER_SCORER=apple</code> (macOS 26+, Apple Silicon) scores on-device with\nApple Intelligence Foundation Models via the shared <code>apple-foundation</code>\nbridge — free, private, no API key. The bridge auto-builds to\n<code>~/.local/share/gobstopper/apple-bridge</code> on first use (or set\n<code>GOBSTOPPER_APPLE_BRIDGE</code>), requests queue through one persistent process\nwith guided JSON output, and any failure degrades to neutral scores.\n<code>GOBSTOPPER_APPLE_TIMEOUT_MS</code>, <code>_MAX_CANDIDATES</code>, <code>_BATCH_SIZE</code>, and\n<code>_MAX_BATCHES</code> tune it. Since inference is local, the scorer also reads a\nbounded excerpt of each candidate record (<code>GOBSTOPPER_APPLE_CONTENT_BYTES</code>,\ndefault 400; <code>0</code> restores labels-only scoring) and shrinks its default\nbatch sizes to fit the ~4k-token context window.</p>\n<p><code>GOBSTOPPER_DIGEST=apple</code> goes further: the injected state card is written\nby the on-device model instead of keyword extraction. Because inference is\nlocal, it may read bounded excerpts of the records being elided — the\nlabels-only boundary only exists for remote endpoints. Each field still\nlands in the same <code>DigestBlock</code> shape via guided output, capped to a small\ntoken overhead, and falls back to the mechanical card on any failure.\n<code>GOBSTOPPER_APPLE_DIGEST_ITEMS</code>, <code>_ITEM_BYTES</code>, and <code>_TOTAL_BYTES</code> tune the\nexcerpt budget, which defaults are sized to the model's ~4k-token window.</p>\n<p>The same call also writes a one-line stub per excerpted record — e.g.\n<code>Script completed Wall time 4.4 seconds</code> — stored in the elide edit's\n<code>per_item_stubs</code> map and rendered verbatim in place of the <code>{bytes}</code>/<code>{kind}</code>\ntemplate where the payload was removed. Records the model did not cover\nkeep the generic stub; invalid or oversized stubs are dropped by validation.</p>\n<p>Apple requests are cached in-process on the (prompt, schema) pair: <code>watch</code>\nre-evaluates an unchanged transcript every poll interval, and identical\nmodel inputs return the recorded response instead of another generation —\na live dry-run poll went from ~17s to milliseconds per re-eval. Bounded at\n64 entries; <code>GOBSTOPPER_APPLE_CACHE=0</code> disables reads. The savings gate\nalso prices the residual stub text left behind by elision so\n<code>context_tokens_after</code> doesn't overstate reclaim.</p>\n<p>With <code>adaptive = true</code>, the effective trigger/floor are re-derived per\nsession at each decision point: the trigger is capped at a quarter of\nthe provider-advertised context window, backed off (bounded 2x) when\nrecent compactions reclaimed too little to be worth a cycle, and\ntightened when most of the window is reclaimable tool output. The\nadjustment is deterministic and its reasons appear in plan output and\ntelemetry. <code>gobstopper tune &lt;session&gt;</code> previews it.</p>\n<h2 id=\"the-oompa-seam\">The oompa seam</h2>\n<p>oompa never parses provider transcript files — that boundary stays intact.\nIntegration is the <code>policy-check</code> subcommand: oompa already records\n<code>token_usage</code> events (<code>totalTokens</code>, <code>modelContextWindow</code>) in its neutral\ntimeline, so it asks gobstopper what to do with them:</p>\n<pre><code class=\"language-sh\">gobstopper policy-check --provider codex --context-tokens 300000 \\\n    --session-active --json\n# {&quot;action&quot;:&quot;provider_compact&quot;,&quot;control&quot;:&quot;thread/compact/start&quot;, ...}\n</code></pre>\n<p>oompa then invokes <code>thread/compact/start</code> on its own app-server\nconnection, or launches Claude sessions with <code>--autocompact &lt;tokens&gt;</code>.\nDeeper transcript surgery stays gobstopper-side and publishes verified forks\nfor explicit resume. It never creates a second writer for an oompa-managed\nsession.</p>\n<h2 id=\"how-providers-compact-today\">How providers compact today</h2>\n<table>\n<thead>\n<tr><th>lever</th><th align=\"right\">codex</th><th align=\"right\">claude code</th></tr>\n</thead>\n<tbody>\n<tr><td>auto-compact threshold</td><td align=\"right\"><code>model_auto_compact_token_limit</code> (config; ≤90% of window)</td><td align=\"right\"><code>--autocompact &lt;100k–1M&gt;</code> argv</td></tr>\n<tr><td>on-demand trigger</td><td align=\"right\"><code>thread/compact/start</code> (app-server v2)</td><td align=\"right\"><code>/compact [instructions]</code></td></tr>\n<tr><td>compaction prompt</td><td align=\"right\"><code>compact_prompt</code> config</td><td align=\"right\"><code>/compact</code> instructions</td></tr>\n<tr><td>tool output cap</td><td align=\"right\"><code>tool_output_token_limit</code></td><td align=\"right\">—</td></tr>\n<tr><td>live usage stream</td><td align=\"right\"><code>thread/tokenUsage/updated</code> notification</td><td align=\"right\"><code>message.usage</code> per turn</td></tr>\n<tr><td>transcript store</td><td align=\"right\"><code>~/.codex/sessions/**/rollout-*.jsonl</code></td><td align=\"right\"><code>~/.claude/projects/*/*.jsonl</code></td></tr>\n</tbody>\n</table>\n<p>Codex persists compaction as a <code>compacted</code> rollout record carrying\n<code>replacement_history</code> — the provider's own resume-time context swap.\ngobstopper's parser and verifier understand that shape, including tool pairs\ninside <code>replacement_history</code>. Synthetic records are experimental, pair-aware,\ntransactional, and available only behind <code>--experimental-compacted</code>; ordinary\n<code>apply</code> uses the portable forked digest representation.</p>\n<h2 id=\"layout\">Layout</h2>\n<ul>\n<li><code>crates/gobstopper-core</code> — normalized transcript model, the <code>Edit</code> IR,\n<code>Strategy</code> trait, and all built-in strategies. No I/O.</li>\n<li><code>crates/gobstopper-adapters</code> — bounded session discovery, Codex/Claude\nJSONL parsing and candidate rewriting, transactional no-clobber publication,\nverification, plugin hosting, and the snapshot vault.</li>\n<li><code>crates/gobstopper-cli</code> — <code>gobstopper</code> binary: detect / plan / apply /\nverify / undo / vault / history / show / recall / diff / bench /\nmcp / watch / policy-check / presets / explain.</li>\n</ul>\n<h2 id=\"live-qualification\">Live qualification</h2>\n<p><code>gobstopper</code> has been live-qualified on real provider sessions. The most\nrecent trial used a 333k-token Claude session, asked the same resume\nquestion under four conditions, and measured the tokens the provider\nactually consumed on the next turn:</p>\n<table>\n<thead>\n<tr><th>condition</th><th align=\"right\">input tokens on resume</th><th align=\"right\">output tokens</th><th align=\"right\">recalled the standing task?</th></tr>\n</thead>\n<tbody>\n<tr><td>none (original)</td><td align=\"right\">312,722</td><td align=\"right\">1,405</td><td align=\"right\">yes — reported npm unification and stalled renames</td></tr>\n<tr><td><code>gobstopper elide</code></td><td align=\"right\">219,167</td><td align=\"right\">1,052</td><td align=\"right\">yes — same standing task, stalled renames</td></tr>\n<tr><td><code>gobstopper compacted</code></td><td align=\"right\">220,447</td><td align=\"right\">621</td><td align=\"right\">yes — same standing task from the state-card digest</td></tr>\n<tr><td><code>claude --autocompact 100</code></td><td align=\"right\">56,300</td><td align=\"right\">416</td><td align=\"right\">no — incorrectly claimed the renames were already done and published</td></tr>\n</tbody>\n</table>\n<p><code>gobstopper elide</code> and <code>compacted</code> both cut the resume context by about\n30% while keeping the answer accurate. Claude's native <code>--autocompact 100</code>\ncut the resume context by ~82% but produced a confident, inaccurate\nsummary of the session.</p>\n<p>The same question was then asked on a 101k-token Codex session:</p>\n<table>\n<thead>\n<tr><th>condition</th><th align=\"right\">input tokens on resume</th><th align=\"right\">output tokens</th><th align=\"right\">recalled the standing task?</th></tr>\n</thead>\n<tbody>\n<tr><td>none (original)</td><td align=\"right\">101,275</td><td align=\"right\">244</td><td align=\"right\">yes — Oh's memory benchmark and the 0.60 expansion gate</td></tr>\n<tr><td><code>gobstopper elide</code></td><td align=\"right\">57,980</td><td align=\"right\">83</td><td align=\"right\">yes — same 0.545 score and 0.60 gate</td></tr>\n<tr><td><code>gobstopper compacted</code></td><td align=\"right\">34,503</td><td align=\"right\">159</td><td align=\"right\">yes — same BEAM experiment and expansion gate</td></tr>\n</tbody>\n</table>\n<p>On Codex, <code>compacted</code> cut resume input tokens by <strong>66%</strong> and <code>elide</code> cut\nthem by <strong>43%</strong>, both with accurate answers. There is no one-shot Codex\nnative compact to compare against.</p>\n<p>The same-session <code>cache_aware</code> A/B (339k-token Claude session, floor 310k,\nreal provider cache counters):</p>\n<table>\n<thead>\n<tr><th>condition</th><th align=\"right\"><code>cache_read</code></th><th align=\"right\"><code>cache_creation</code></th><th align=\"right\">file-level prefix preserved</th><th align=\"right\">cost</th><th>accurate?</th></tr>\n</thead>\n<tbody>\n<tr><td>none (original)</td><td align=\"right\">10,010</td><td align=\"right\">325,647</td><td align=\"right\">—</td><td align=\"right\">$6.53</td><td>yes</td></tr>\n<tr><td><code>gobstopper cache_aware</code></td><td align=\"right\">13,536</td><td align=\"right\">258,517</td><td align=\"right\">107,884 tokens</td><td align=\"right\">$5.19</td><td>yes</td></tr>\n<tr><td><code>gobstopper compacted</code></td><td align=\"right\">13,536</td><td align=\"right\">257,505</td><td align=\"right\">6,639 tokens</td><td align=\"right\">$5.18</td><td>yes</td></tr>\n</tbody>\n</table>\n<p>Honest result: <code>cache_aware</code> and <code>compacted</code> cost the same on the API —\nClaude Code's prompt-cache breakpoints sit at ~13.5k regardless of how much\nfile-level prefix stays byte-identical. What <code>cache_aware</code> actually buys is\n<strong>16x more preserved prefix at the transcript level</strong>, which is what keeps\n<code>gobstopper diff</code> audits small and Merkle-dedup efficient across repeated\ncompactions. Choose it when auditability matters; choose <code>compacted</code> when\nyou want the Codex-native record.</p>\n<p>That is the difference gobstopper is built for: measured, auditable\ncompaction that does not replace the transcript's actual state with a\nplausible invention. Every pre- and post-state is in the vault, so you can\n<code>gobstopper diff</code> the exact structural changes and decide which strategy to\ntrust.</p>\n<ul>\n<li><strong>Codex custom <code>compacted</code> record</strong> — a gobstopper-written <code>compacted</code>\nrecord with a correct window chain was accepted by <code>codex exec resume</code> and\nthe model completed a real API turn recalling the elided commands.</li>\n<li><strong>Claude Code digest resume</strong> — an earlier qualification trial on a separate\n333k-token test copy elided 43 stale tool records and injected a state card;\n<code>claude --resume</code> succeeded and recalled the standing task. Current releases\npreserve that evidence while exposing standalone application only through\nno-clobber fork publication.</li>\n</ul>\n<h2 id=\"status\">Status</h2>\n<p>The published safety posture is conservative: <code>plan</code>, <code>eval</code>, <code>verify</code>, MCP,\nand provider inspection are read-only; standalone <code>apply</code>, <code>watch</code>, and <code>undo</code>\npublish separate files and never overwrite a provider-owned source. Candidate\npublication is source-hash-bound, no-clobber, snapshotted, structurally\nverified, idempotent through durable receipts, and bounded to 128 MiB/100,000\nrecords. The default deterministic strategies and built-in Codex/Claude\nadapters are covered by unit, regression, property, and live-resume evidence.</p>\n<p>Direct provider controls still belong to the live session owner. Synthetic\nCodex <code>compacted</code> records, external model scoring, and semantic editor plugins\nremain explicitly experimental or trusted extension paths. Devin support is a\nnative numeric policy/MCP handoff to <code>/compact</code>, not direct transcript surgery.\nSee <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/design.md\">docs/design.md</a>, <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/roadmap.md\">docs/roadmap.md</a>,\n<a href=\"https://github.com/hraness/gobstopper/blob/main/docs/plugin-protocol.md\">docs/plugin-protocol.md</a>, and\n<a href=\"https://github.com/hraness/gobstopper/blob/main/docs/devin.md\">docs/devin.md</a> for the boundaries.</p>\n<h2 id=\"license\">License</h2>\n<p>MIT OR Apache-2.0</p>\n";
