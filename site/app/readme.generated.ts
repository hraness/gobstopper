// Generated from ../README.md by scripts/sync-readme.ts. Do not edit.
export const readmeTitle = "gobstopper";
export const readmeLead = "Automatic context compaction for coding-agent sessions — Codex and Claude Code today, any JSONL-transcript agent tomorrow.";
export const readmeHtml = "<h1 id=\"gobstopper\">gobstopper</h1>\n<p>Automatic context compaction for coding-agent sessions — Codex and Claude\nCode today, any JSONL-transcript agent tomorrow.</p>\n<p>gobstopper is the first cross-provider context compactor that preserves a\ncontent-addressed archive of every conversation state, supports resumable\nin-place compaction for both Claude Code and Codex, and gives you a\nfine-grained structural diff of what actually changed.</p>\n<p>Run it, and it watches your agent sessions. When a session's context\ncrosses a configured threshold, gobstopper compacts it — earlier and\nsmarter than the provider's own defaults — using a strategy you choose per\nsession, per provider, or per preset.</p>\n<h2 id=\"why\">Why</h2>\n<p>Providers compact late. Codex fires auto-compaction at ~90% of the model's\ncontext window; Claude Code similar. On a 1M-token window that means every\nturn near the end of a long session costs ~900k input tokens — and on\nsubscription plans, those tokens come out of your weekly allowance.</p>\n<p>Context is a sawtooth problem. If you compact at trigger <code>T</code> and the\nsummary floor is <code>F</code>, steady-state <em>context occupancy</em> per turn is roughly\n<code>(T+F)/2</code>:</p>\n<table>\n<thead>\n<tr><th>policy</th><th>trigger</th><th>floor</th><th>avg context/turn</th><th>relative occupancy</th></tr>\n</thead>\n<tbody>\n<tr><td>provider default (1M window)</td><td>~900k</td><td>~60k</td><td>~480k</td><td>1.0x</td></tr>\n<tr><td>gobstopper default</td><td>250k</td><td>40k</td><td>~145k</td><td>~3.3x lower occupancy</td></tr>\n<tr><td>gobstopper aggressive</td><td>150k</td><td>20k</td><td>~85k</td><td>~5.6x lower occupancy</td></tr>\n</tbody>\n</table>\n<p>This is an <strong>occupancy model</strong>, not a measured subscription savings claim.\nActual token cost depends on cache hit rates, whether summary turns are\nbilled, re-fetches caused by lost detail, and how often compaction itself\nruns. Anthropic documents that clearing tool results can\n<a href=\"https://platform.claude.com/docs/en/build-with-claude/context-editing\">invalidate the prompt cache</a>.\ngobstopper reports measured file-byte changes and observed provider usage\nwhere available; it does not project dollar or quota savings into its\npublic claims.</p>\n<p>Compaction itself isn't free — each cycle costs one large input call and\nrisks losing detail — so strategy matters. That is the actual product\nhere: not &quot;compact earlier&quot; but &quot;compact with the right strategy at the\nright boundary.&quot;</p>\n<h2 id=\"strategies\">Strategies</h2>\n<table>\n<thead>\n<tr><th>id</th><th>kind</th><th>what it does</th></tr>\n</thead>\n<tbody>\n<tr><td><code>auto</code> (default)</td><td>dynamic</td><td>selects per-session from transcript composition: tool-heavy → <code>elide</code>, chatty → <code>structured</code>, live/empty → <code>sawtooth</code></td></tr>\n<tr><td><code>sawtooth</code></td><td>provider</td><td>fires the provider's own compaction early (<code>thread/compact/start</code> on Codex app-server; <code>/compact</code> or <code>--autocompact</code> on Claude)</td></tr>\n<tr><td><code>elide</code></td><td>transcript</td><td>stubs stale tool outputs oldest-first until the floor; deterministic, no model call</td></tr>\n<tr><td><code>structured</code></td><td>transcript</td><td>placeholder state-card digest (<code>goal</code>/<code>decisions</code>/<code>files</code>/<code>todos</code>); currently emits item labels, not a real summary. Safe for chat-only sessions but should not be mistaken for a semantic compressor</td></tr>\n<tr><td><code>agentic</code></td><td>transcript</td><td>reserved for a bounded editor-model backend; today <code>preset.command</code> is the only extension point and is treated as untrusted code</td></tr>\n</tbody>\n</table>\n<p>Custom strategies are userspace code: a preset can name a <code>command</code> that\nreceives the normalized transcript as JSON on stdin and returns an edit\nplan on stdout, or install a versioned <code>gobstopper-plugin.json</code> bundle\n(see <code>gobstopper plugin check</code>). Host-side validation bounds every\nproposal: no edit can grow the transcript, leave protected recent output,\nbypass linkage checks, or exceed configured digest size.</p>\n<h2 id=\"install--use\">Install &amp; use</h2>\n<pre><code class=\"language-sh\">cargo install --git https://github.com/hraness/gobstopper gobstopper\n# or from a checkout: cargo build --release\n\ngobstopper detect                  # sessions, context sizes, lifetime burn\ngobstopper plan &lt;session&gt;          # what would happen, under which strategy\ngobstopper eval &lt;session&gt;          # every strategy side-by-side on temp copies\ngobstopper apply &lt;session&gt;         # vault snapshot + produce validated fork (idle sessions)\ngobstopper verify &lt;session&gt;        # resume-validity check (exit 1 on errors)\ngobstopper fork &lt;session&gt;          # clone under a fresh session id + resume cmd\ngobstopper undo &lt;session&gt;          # restore a pre-compaction snapshot into a new fork\ngobstopper vault                   # list snapshots in the undo vault\ngobstopper install-hooks           # Claude + Codex compaction lifecycle hooks\ngobstopper watch --dry-run         # the daemon path: poll, threshold, prepare copy\ngobstopper explain                 # the occupancy math above\n</code></pre>\n<p>Every <code>apply</code>/<code>watch</code> compaction snapshots the source transcript into a\ncontent-addressed vault (<code>~/.local/share/gobstopper/vault/</code>) and publishes\nthe result as a separate, verified file. The original transcript is never\noverwritten by a standalone compaction run; live session surgery must be\ndispatched by the session owner. Each compaction appends a numeric record\nto <code>events.jsonl</code> in the <code>gobstopper/compaction-events-v1</code> schema.</p>\n<p>Config: <code>~/.config/gobstopper/config.toml</code></p>\n<pre><code class=\"language-toml\">[policy]\nstrategy = &quot;auto&quot;            # sawtooth | elide | structured | agentic\ntrigger_tokens = 250_000\nfloor_tokens = 40_000\n\n[provider.codex]             # per-provider overrides\ntrigger_tokens = 200_000\n\n[sessions.&quot;01a08d7c-…&quot;]      # per-session overrides\nstrategy = &quot;structured&quot;\ntrigger_tokens = 120_000\n\n[presets.deep-work]          # named presets, selectable via --preset\nstrategy = &quot;elide&quot;\ntrigger_tokens = 150_000\n\n[presets.custom-script]      # userspace code preset\ncommand = &quot;python3 ~/bin/my_compactor.py&quot;\n</code></pre>\n<p>Managed sessions (oompa profiles, sandboxed homes) use different roots:\npoint gobstopper at them with <code>--codex-home</code> / <code>--claude-home</code>.</p>\n<h2 id=\"the-oompa-seam\">The oompa seam</h2>\n<p>oompa never parses provider transcript files — that boundary stays intact.\nIntegration is the <code>policy-check</code> subcommand: oompa already records\n<code>token_usage</code> events (<code>totalTokens</code>, <code>modelContextWindow</code>) in its neutral\ntimeline, so it asks gobstopper what to do with them:</p>\n<pre><code class=\"language-sh\">gobstopper policy-check --provider codex --context-tokens 300000 \\\n    --session-active --json\n# {&quot;action&quot;:&quot;provider_compact&quot;,&quot;control&quot;:&quot;thread/compact/start&quot;, ...}\n</code></pre>\n<p>oompa then invokes <code>thread/compact/start</code> on its own app-server\nconnection, or launches Claude sessions with <code>--autocompact &lt;tokens&gt;</code>.\nDeeper transcript surgery (elide/structured) stays gobstopper-side,\napplied to idle sessions or at resume boundaries.</p>\n<h2 id=\"how-providers-compact-today\">How providers compact today</h2>\n<table>\n<thead>\n<tr><th>lever</th><th>codex</th><th>claude code</th></tr>\n</thead>\n<tbody>\n<tr><td>auto-compact threshold</td><td><code>model_auto_compact_token_limit</code> (config; ≤90% of window)</td><td><code>--autocompact &lt;100k–1M&gt;</code> argv</td></tr>\n<tr><td>on-demand trigger</td><td><code>thread/compact/start</code> (app-server v2)</td><td><code>/compact [instructions]</code></td></tr>\n<tr><td>compaction prompt</td><td><code>compact_prompt</code> config</td><td><code>/compact</code> instructions</td></tr>\n<tr><td>tool output cap</td><td><code>tool_output_token_limit</code></td><td>—</td></tr>\n<tr><td>live usage stream</td><td><code>thread/tokenUsage/updated</code> notification</td><td><code>message.usage</code> per turn</td></tr>\n<tr><td>transcript store</td><td><code>~/.codex/sessions/**/rollout-*.jsonl</code></td><td><code>~/.claude/projects/*/*.jsonl</code></td></tr>\n</tbody>\n</table>\n<p>Codex persists compaction as a <code>compacted</code> rollout record carrying\n<code>replacement_history</code> — the provider's own resume-time context swap.\ngobstopper's transcript path respects that shape (elision inside\n<code>replacement_history</code> is supported); writing custom <code>compacted</code> records\nfor fully custom summaries is the designed v0.2 path.</p>\n<h2 id=\"layout\">Layout</h2>\n<ul>\n<li><code>crates/gobstopper-core</code> — normalized transcript model, the <code>Edit</code> IR,\n<code>Strategy</code> trait, and all built-in strategies. No I/O.</li>\n<li><code>crates/gobstopper-adapters</code> — session discovery and the Codex/Claude\nJSONL dialects (parse + in-place rewrite; lines are never removed, so\nprovider linkage is preserved).</li>\n<li><code>crates/gobstopper-cli</code> — <code>gobstopper</code> binary: detect / plan / apply /\nverify / undo / vault / watch / policy-check / presets / explain.</li>\n</ul>\n<h2 id=\"live-qualification\">Live qualification</h2>\n<p><code>gobstopper</code> has been live-qualified on real provider sessions:</p>\n<ul>\n<li><strong>Codex custom <code>compacted</code> record</strong> — a gobstopper-written <code>compacted</code>\nrecord with a correct window chain was accepted by <code>codex exec resume</code> and\nthe model completed a real API turn recalling the elided commands.</li>\n<li><strong>Claude Code <code>compacted</code> / digest resume</strong> — <code>gobstopper apply --in-place --strategy compacted</code> on a 333k-token Claude session elided 43\nstale tool records and injected a state-card digest. <code>gobstopper diff</code>\nagainst the original vault snapshot showed exactly 43 removed and 46 added\nrecords (43 stubs + 3 tail records); <code>claude --resume &lt;session&gt;</code> succeeded\nand the model recalled the last user prompt and current task state from the\ndigest.</li>\n<li><strong>Native comparison</strong> — running <code>claude --resume &lt;session&gt; --autocompact 100</code> on the same session appended 59 records but removed 0 existing records;\ngobstopper was the only one to produce a measurable structural reduction.</li>\n</ul>\n<p>These are bounded, single-session results, not a broad benchmark, but they\nshow that gobstopper's in-place compaction is resumable and that it can\nreduce transcripts that the provider's own auto-compaction left unchanged.</p>\n<h2 id=\"status\">Status</h2>\n<p>Production-ready for idle and resume-boundary transcript compaction on Codex\nand Claude Code. Core detection, planning, and transcript surgery are covered\nby a property-tested suite plus the live qualifications above. <code>apply</code> and\n<code>watch</code> always snapshot the source into the content-addressed vault first;\n<code>verify</code> checks resume-validity, <code>undo</code> restores to a new fork, and <code>vault</code>\nkeeps every state. <code>sawtooth</code> can route to Codex's <code>thread/compact/start</code>\nover a private app-server connection when <code>codex-cli</code> is installed and\ntrusted.</p>\n<p>The <code>structured</code> and <code>agentic</code> strategies remain conservative placeholders or\nextension points, not proven semantic compressors. Custom strategy code is\ntreated as untrusted and validated by the host before any transcript is\nwritten. See <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/design.md\">docs/design.md</a> for the research record and <a href=\"https://github.com/hraness/gobstopper/blob/main/docs/roadmap.md\">docs/roadmap.md</a> for the full plan.</p>\n<h2 id=\"license\">License</h2>\n<p>MIT OR Apache-2.0</p>\n";
